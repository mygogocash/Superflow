// GENERATED by `pnpm --filter @nexora/db db:pull`. Do not hand-edit.
import { relations } from "drizzle-orm/relations";
import {
  projects,
  approvalChainDecisions,
  proposals,
  users,
  conversations,
  messages,
  wallPosts,
  wallComments,
  companyNews,
  companyDates,
  manutAiConversations,
  manutAiConversationSummaries,
  manutAiConversationMemory,
  manutAiMessages,
  manutAiAttachments,
  manutAiKnowledgeArticles,
  manutAiQueryLogs,
  manutAiBriefSubscriptions,
  manutAiBriefDeliveries,
  blogs,
  manutAiFeedback,
  wikiPages,
  wikiPageVersions,
  wikiPagePermissions,
  userEntityMemberships,
  entities,
  roles,
  articles,
  departments,
  chartOfAccounts,
  journalEntries,
  authLogs,
  vendors,
  bankTransactions,
  bankAccounts,
  payments,
  cashAdvanceRequests,
  invoices,
  invoiceLineItems,
  cashAdvanceApprovalSteps,
  journalEntryLines,
  expenses,
  expenseCategories,
  travelRequests,
  expenseReports,
  expenseApprovalSteps,
  expenseApprovalDecisions,
  paymentAllocations,
  cashAdvanceApprovalDecisions,
  quotes,
  quoteLines,
  taxCodes,
  cashAdvanceItems,
  creditNotes,
  creditNoteLines,
  fiscalPeriods,
  purchaseOrders,
  accountMappings,
  documentSequences,
  poLines,
  fixedAssets,
  fixedAssetTransfers,
  helpdeskTickets,
  validatorNodeAlerts,
  helpdeskSettings,
  helpdeskComments,
  fixedAssetDisposals,
  fixedAssetRemeasurements,
  surveyForms,
  surveyFormResponses,
  surveys,
  surveyQuestions,
  surveyResponses,
  travelApprovalSteps,
  travelApprovalDecisions,
  leaveTypes,
  surveyFormQuestions,
  surveyFormAnswers,
  surveyAnswers,
  balanceTransactions,
  leaveRequests,
  leaveApprovalSteps,
  leaveApprovalDecisions,
  publicHolidays,
  payrollRuns,
  payslips,
  consultantInvoices,
  esopGrants,
  leavePolicyApprovers,
  leaveBalances,
  onboardingRuns,
  offboardingRuns,
  visaRecords,
  visaEventLogs,
  visaKnowledgeArticles,
  visaChecklistItems,
  employeeAgreements,
  benefits,
  benefitEnrollments,
  companyPolicies,
  payrollApprovalSteps,
  attendancePolicies,
  attendanceCorrections,
  attendanceRecords,
  attendanceEmployeeShifts,
  attendanceShifts,
  attendanceAuditLogs,
  ninetyDayNotifications,
  attendanceExceptions,
  userGoogleConnections,
  investors,
  investorTasks,
  investorActivities,
  investorLeads,
  investorAccounts,
  certificates,
  investments,
  dataRoomDocuments,
  investorUpdates,
  itVendors,
  itSubscriptions,
  itBillingRecords,
  itBillingAlerts,
  investorContacts,
  itAccessRequests,
  itAccessAssignments,
  itSystems,
  itAccessAuditLogs,
  legalDocuments,
  legalDocumentShares,
  userGroups,
  legalDocumentAttachments,
  legalSignatures,
  userDocusignConnections,
  legalAnnouncements,
  legalAnnouncementAttachments,
  itAccessApprovalDecisions,
  mktCampaigns,
  mktCampaignLevers,
  mktLevers,
  mktCreatives,
  mktPredictions,
  partners,
  partnerContacts,
  partnerMembers,
  partnerColumns,
  partnerTasks,
  partnerTaskComments,
  partnerTaskAssignees,
  projectWorkflowTransitions,
  projectWorkflowEmails,
  projectMembers,
  projectColumns,
  projectTasks,
  projectMilestones,
  projectTaskComments,
  partnerTaskResources,
  deals,
  projectTaskAssignees,
  projectTaskActivities,
  projectTaskResources,
  itProjects,
  itProjectMembers,
  itProjectColumns,
  itProjectTasks,
  itProjectTaskComments,
  itProjectTaskAssignees,
  projectTaskDependencies,
  qaProjects,
  qaProjectMembers,
  qaProjectColumns,
  qaProjectTasks,
  qaProjectTaskComments,
  qaProjectTaskAssignees,
  legalProjects,
  legalProjectMembers,
  legalProjectColumns,
  legalProjectTasks,
  legalProjectTaskComments,
  legalProjectTaskAssignees,
  accountingProjects,
  accountingProjectColumns,
  accountingProjectTasks,
  accountingProjectTaskComments,
  accountingProjectTaskAssignees,
  productProjects,
  productProjectMembers,
  productProjectColumns,
  productProjectTasks,
  productProjectTaskComments,
  productProjectTaskAssignees,
  accountingProjectMembers,
  officeDesks,
  deskBookings,
  meetingRooms,
  roomBookings,
  offices,
  assets,
  voucherEntries,
  appraisalCycles,
  marketingCampaigns,
  appraisals,
  appraisalKras,
  kraTemplates,
  appraisalComments,
  appraisalRatings,
  goals,
  proposalInformationRequests,
  proposalTransitions,
  proposalEmails,
  crmAccounts,
  crmContacts,
  crmLeads,
  crmOpportunities,
  crmActivities,
  moduleOwners,
  crmSettings,
  crmOpportunityBusinessUnits,
  revenueAccounts,
  revenueContacts,
  revenueLeads,
  revenueOpportunities,
  revenueActivities,
  crmTasks,
  revenueSettings,
  revenueOpportunityBusinessUnits,
  auditLog,
  approvalChains,
  approvalChainSteps,
  jobs,
  applications,
  sessions,
  fixedAssetCountSessions,
  fixedAssetCountLines,
  revenueTasks,
  rolePermissions,
  messageHiddenFor,
  messageReactions,
  trainingCompletions,
  trainingModules,
  legalAnnouncementAcks,
  userRoles,
  userGroupMembers,
  moduleAccess,
  conversationMembers,
} from "./schema";

export const approvalChainDecisionsRelations = relations(
  approvalChainDecisions,
  ({ one }) => ({
    project: one(projects, {
      fields: [approvalChainDecisions.projectId],
      references: [projects.id],
    }),
    proposal: one(proposals, {
      fields: [approvalChainDecisions.proposalId],
      references: [proposals.id],
    }),
    user_approverUserId: one(users, {
      fields: [approvalChainDecisions.approverUserId],
      references: [users.id],
      relationName: "approvalChainDecisions_approverUserId_users_id",
    }),
    user_decidedById: one(users, {
      fields: [approvalChainDecisions.decidedById],
      references: [users.id],
      relationName: "approvalChainDecisions_decidedById_users_id",
    }),
  }),
);

export const projectsRelations = relations(projects, ({ one, many }) => ({
  approvalChainDecisions: many(approvalChainDecisions),
  projectWorkflowTransitions: many(projectWorkflowTransitions),
  projectWorkflowEmails: many(projectWorkflowEmails),
  projectMembers: many(projectMembers),
  projectColumns: many(projectColumns),
  projectTasks: many(projectTasks),
  user: one(users, {
    fields: [projects.ownerId],
    references: [users.id],
  }),
  partner: one(partners, {
    fields: [projects.partnerId],
    references: [partners.id],
  }),
  projectMilestones: many(projectMilestones),
  proposals: many(proposals),
}));

export const proposalsRelations = relations(proposals, ({ one, many }) => ({
  approvalChainDecisions: many(approvalChainDecisions),
  proposalInformationRequests: many(proposalInformationRequests),
  proposalTransitions: many(proposalTransitions),
  proposalEmails: many(proposalEmails),
  project: one(projects, {
    fields: [proposals.projectId],
    references: [projects.id],
  }),
}));

export const usersRelations = relations(users, ({ one, many }) => ({
  approvalChainDecisions_approverUserId: many(approvalChainDecisions, {
    relationName: "approvalChainDecisions_approverUserId_users_id",
  }),
  approvalChainDecisions_decidedById: many(approvalChainDecisions, {
    relationName: "approvalChainDecisions_decidedById_users_id",
  }),
  conversations: many(conversations),
  messages_authorId: many(messages, {
    relationName: "messages_authorId_users_id",
  }),
  messages_deletedBy: many(messages, {
    relationName: "messages_deletedBy_users_id",
  }),
  wallComments: many(wallComments),
  companyNews: many(companyNews),
  companyDates: many(companyDates),
  wallPosts: many(wallPosts),
  manutAiConversations: many(manutAiConversations),
  manutAiAttachments: many(manutAiAttachments),
  manutAiKnowledgeArticles: many(manutAiKnowledgeArticles),
  manutAiQueryLogs: many(manutAiQueryLogs),
  manutAiBriefSubscriptions: many(manutAiBriefSubscriptions),
  manutAiBriefDeliveries: many(manutAiBriefDeliveries),
  blogs: many(blogs),
  manutAiFeedbacks_userId: many(manutAiFeedback, {
    relationName: "manutAiFeedback_userId_users_id",
  }),
  manutAiFeedbacks_reviewedById: many(manutAiFeedback, {
    relationName: "manutAiFeedback_reviewedById_users_id",
  }),
  wikiPages_createdById: many(wikiPages, {
    relationName: "wikiPages_createdById_users_id",
  }),
  wikiPages_updatedById: many(wikiPages, {
    relationName: "wikiPages_updatedById_users_id",
  }),
  wikiPageVersions: many(wikiPageVersions),
  wikiPagePermissions: many(wikiPagePermissions),
  userEntityMemberships: many(userEntityMemberships),
  articles: many(articles),
  entity: one(entities, {
    fields: [users.entityId],
    references: [entities.id],
  }),
  user: one(users, {
    fields: [users.reportingTo],
    references: [users.id],
    relationName: "users_reportingTo_users_id",
  }),
  users: many(users, {
    relationName: "users_reportingTo_users_id",
  }),
  journalEntries_createdBy: many(journalEntries, {
    relationName: "journalEntries_createdBy_users_id",
  }),
  journalEntries_approvedBy: many(journalEntries, {
    relationName: "journalEntries_approvedBy_users_id",
  }),
  journalEntries_rejectedBy: many(journalEntries, {
    relationName: "journalEntries_rejectedBy_users_id",
  }),
  journalEntries_cancelledBy: many(journalEntries, {
    relationName: "journalEntries_cancelledBy_users_id",
  }),
  authLogs: many(authLogs),
  cashAdvanceRequests_employeeId: many(cashAdvanceRequests, {
    relationName: "cashAdvanceRequests_employeeId_users_id",
  }),
  cashAdvanceRequests_approvedBy: many(cashAdvanceRequests, {
    relationName: "cashAdvanceRequests_approvedBy_users_id",
  }),
  cashAdvanceApprovalSteps: many(cashAdvanceApprovalSteps),
  expenses_employeeId: many(expenses, {
    relationName: "expenses_employeeId_users_id",
  }),
  expenses_approvedBy: many(expenses, {
    relationName: "expenses_approvedBy_users_id",
  }),
  expenseReports_employeeId: many(expenseReports, {
    relationName: "expenseReports_employeeId_users_id",
  }),
  expenseReports_approvedBy: many(expenseReports, {
    relationName: "expenseReports_approvedBy_users_id",
  }),
  expenseApprovalSteps: many(expenseApprovalSteps),
  expenseApprovalDecisions_approverUserId: many(expenseApprovalDecisions, {
    relationName: "expenseApprovalDecisions_approverUserId_users_id",
  }),
  expenseApprovalDecisions_decidedById: many(expenseApprovalDecisions, {
    relationName: "expenseApprovalDecisions_decidedById_users_id",
  }),
  cashAdvanceApprovalDecisions_approverUserId: many(
    cashAdvanceApprovalDecisions,
    {
      relationName: "cashAdvanceApprovalDecisions_approverUserId_users_id",
    },
  ),
  cashAdvanceApprovalDecisions_decidedById: many(cashAdvanceApprovalDecisions, {
    relationName: "cashAdvanceApprovalDecisions_decidedById_users_id",
  }),
  helpdeskTickets_createdBy: many(helpdeskTickets, {
    relationName: "helpdeskTickets_createdBy_users_id",
  }),
  helpdeskTickets_assigneeId: many(helpdeskTickets, {
    relationName: "helpdeskTickets_assigneeId_users_id",
  }),
  validatorNodeAlerts: many(validatorNodeAlerts),
  helpdeskSettings: many(helpdeskSettings),
  helpdeskComments: many(helpdeskComments),
  surveyFormResponses: many(surveyFormResponses),
  surveys: many(surveys),
  surveyResponses: many(surveyResponses),
  travelApprovalSteps: many(travelApprovalSteps),
  surveyForms: many(surveyForms),
  travelApprovalDecisions_approverUserId: many(travelApprovalDecisions, {
    relationName: "travelApprovalDecisions_approverUserId_users_id",
  }),
  travelApprovalDecisions_decidedById: many(travelApprovalDecisions, {
    relationName: "travelApprovalDecisions_decidedById_users_id",
  }),
  travelRequests_employeeId: many(travelRequests, {
    relationName: "travelRequests_employeeId_users_id",
  }),
  travelRequests_approvedBy: many(travelRequests, {
    relationName: "travelRequests_approvedBy_users_id",
  }),
  travelRequests_delegatedTo: many(travelRequests, {
    relationName: "travelRequests_delegatedTo_users_id",
  }),
  balanceTransactions: many(balanceTransactions),
  leaveRequests_employeeId: many(leaveRequests, {
    relationName: "leaveRequests_employeeId_users_id",
  }),
  leaveRequests_approvedBy: many(leaveRequests, {
    relationName: "leaveRequests_approvedBy_users_id",
  }),
  leaveRequests_delegatedTo: many(leaveRequests, {
    relationName: "leaveRequests_delegatedTo_users_id",
  }),
  leaveApprovalSteps: many(leaveApprovalSteps),
  leaveApprovalDecisions_approverUserId: many(leaveApprovalDecisions, {
    relationName: "leaveApprovalDecisions_approverUserId_users_id",
  }),
  leaveApprovalDecisions_decidedById: many(leaveApprovalDecisions, {
    relationName: "leaveApprovalDecisions_decidedById_users_id",
  }),
  payrollRuns_runBy: many(payrollRuns, {
    relationName: "payrollRuns_runBy_users_id",
  }),
  payrollRuns_approvedBy: many(payrollRuns, {
    relationName: "payrollRuns_approvedBy_users_id",
  }),
  payslips: many(payslips),
  consultantInvoices: many(consultantInvoices),
  esopGrants: many(esopGrants),
  leavePolicyApprovers: many(leavePolicyApprovers),
  leaveBalances: many(leaveBalances),
  onboardingRuns: many(onboardingRuns),
  offboardingRuns: many(offboardingRuns),
  visaRecords: many(visaRecords),
  visaEventLogs: many(visaEventLogs),
  visaKnowledgeArticles: many(visaKnowledgeArticles),
  employeeAgreements_employeeId: many(employeeAgreements, {
    relationName: "employeeAgreements_employeeId_users_id",
  }),
  employeeAgreements_uploadedById: many(employeeAgreements, {
    relationName: "employeeAgreements_uploadedById_users_id",
  }),
  benefitEnrollments: many(benefitEnrollments),
  companyPolicies: many(companyPolicies),
  payrollApprovalSteps: many(payrollApprovalSteps),
  attendanceCorrections_employeeId: many(attendanceCorrections, {
    relationName: "attendanceCorrections_employeeId_users_id",
  }),
  attendanceCorrections_approvedBy: many(attendanceCorrections, {
    relationName: "attendanceCorrections_approvedBy_users_id",
  }),
  attendanceRecords: many(attendanceRecords),
  attendanceEmployeeShifts: many(attendanceEmployeeShifts),
  attendanceAuditLogs_employeeId: many(attendanceAuditLogs, {
    relationName: "attendanceAuditLogs_employeeId_users_id",
  }),
  attendanceAuditLogs_actorId: many(attendanceAuditLogs, {
    relationName: "attendanceAuditLogs_actorId_users_id",
  }),
  ninetyDayNotifications: many(ninetyDayNotifications),
  attendanceExceptions_employeeId: many(attendanceExceptions, {
    relationName: "attendanceExceptions_employeeId_users_id",
  }),
  attendanceExceptions_approvedBy: many(attendanceExceptions, {
    relationName: "attendanceExceptions_approvedBy_users_id",
  }),
  userGoogleConnections: many(userGoogleConnections),
  investors: many(investors),
  investorTasks: many(investorTasks),
  investorActivities: many(investorActivities),
  investorLeads: many(investorLeads),
  investorAccounts: many(investorAccounts),
  certificates_recipientId: many(certificates, {
    relationName: "certificates_recipientId_users_id",
  }),
  certificates_issuedById: many(certificates, {
    relationName: "certificates_issuedById_users_id",
  }),
  dataRoomDocuments: many(dataRoomDocuments),
  investorUpdates: many(investorUpdates),
  itVendors: many(itVendors),
  itSubscriptions_ownerUserId: many(itSubscriptions, {
    relationName: "itSubscriptions_ownerUserId_users_id",
  }),
  itSubscriptions_createdBy: many(itSubscriptions, {
    relationName: "itSubscriptions_createdBy_users_id",
  }),
  itSubscriptions_renewalDecisionBy: many(itSubscriptions, {
    relationName: "itSubscriptions_renewalDecisionBy_users_id",
  }),
  itBillingRecords: many(itBillingRecords),
  itBillingAlerts: many(itBillingAlerts),
  investorContacts: many(investorContacts),
  itAccessAssignments_employeeId: many(itAccessAssignments, {
    relationName: "itAccessAssignments_employeeId_users_id",
  }),
  itAccessAssignments_grantedBy: many(itAccessAssignments, {
    relationName: "itAccessAssignments_grantedBy_users_id",
  }),
  itAccessAssignments_revokedBy: many(itAccessAssignments, {
    relationName: "itAccessAssignments_revokedBy_users_id",
  }),
  itAccessAuditLogs_userId: many(itAccessAuditLogs, {
    relationName: "itAccessAuditLogs_userId_users_id",
  }),
  itAccessAuditLogs_targetUserId: many(itAccessAuditLogs, {
    relationName: "itAccessAuditLogs_targetUserId_users_id",
  }),
  legalDocuments: many(legalDocuments),
  legalDocumentShares_userId: many(legalDocumentShares, {
    relationName: "legalDocumentShares_userId_users_id",
  }),
  legalDocumentShares_createdById: many(legalDocumentShares, {
    relationName: "legalDocumentShares_createdById_users_id",
  }),
  legalDocumentAttachments: many(legalDocumentAttachments),
  legalSignatures: many(legalSignatures),
  userDocusignConnections: many(userDocusignConnections),
  legalAnnouncements: many(legalAnnouncements),
  itAccessRequests_employeeId: many(itAccessRequests, {
    relationName: "itAccessRequests_employeeId_users_id",
  }),
  itAccessRequests_grantedBy: many(itAccessRequests, {
    relationName: "itAccessRequests_grantedBy_users_id",
  }),
  itAccessApprovalDecisions_approverUserId: many(itAccessApprovalDecisions, {
    relationName: "itAccessApprovalDecisions_approverUserId_users_id",
  }),
  itAccessApprovalDecisions_decidedById: many(itAccessApprovalDecisions, {
    relationName: "itAccessApprovalDecisions_decidedById_users_id",
  }),
  mktCreatives: many(mktCreatives),
  mktPredictions: many(mktPredictions),
  partners: many(partners),
  partnerMembers: many(partnerMembers),
  mktCampaigns_ownerId: many(mktCampaigns, {
    relationName: "mktCampaigns_ownerId_users_id",
  }),
  mktCampaigns_createdBy: many(mktCampaigns, {
    relationName: "mktCampaigns_createdBy_users_id",
  }),
  partnerTaskComments: many(partnerTaskComments),
  partnerTaskAssignees: many(partnerTaskAssignees),
  partnerTasks: many(partnerTasks),
  projectMembers: many(projectMembers),
  projectTasks: many(projectTasks),
  projectTaskComments: many(projectTaskComments),
  projects: many(projects),
  deals: many(deals),
  projectTaskAssignees: many(projectTaskAssignees),
  projectTaskActivities: many(projectTaskActivities),
  projectTaskResources: many(projectTaskResources),
  itProjects: many(itProjects),
  itProjectMembers: many(itProjectMembers),
  itProjectTasks: many(itProjectTasks),
  itProjectTaskComments: many(itProjectTaskComments),
  itProjectTaskAssignees: many(itProjectTaskAssignees),
  projectMilestones: many(projectMilestones),
  qaProjectMembers: many(qaProjectMembers),
  qaProjectTasks: many(qaProjectTasks),
  qaProjectTaskComments: many(qaProjectTaskComments),
  qaProjectTaskAssignees: many(qaProjectTaskAssignees),
  legalProjectMembers: many(legalProjectMembers),
  legalProjectTasks: many(legalProjectTasks),
  legalProjectTaskComments: many(legalProjectTaskComments),
  legalProjectTaskAssignees: many(legalProjectTaskAssignees),
  qaProjects: many(qaProjects),
  legalProjects: many(legalProjects),
  accountingProjectTasks: many(accountingProjectTasks),
  accountingProjectTaskComments: many(accountingProjectTaskComments),
  accountingProjectTaskAssignees: many(accountingProjectTaskAssignees),
  productProjectMembers: many(productProjectMembers),
  productProjectTasks: many(productProjectTasks),
  productProjectTaskComments: many(productProjectTaskComments),
  productProjectTaskAssignees: many(productProjectTaskAssignees),
  accountingProjects: many(accountingProjects),
  accountingProjectMembers: many(accountingProjectMembers),
  productProjects: many(productProjects),
  deskBookings: many(deskBookings),
  roomBookings: many(roomBookings),
  assets: many(assets),
  voucherEntries: many(voucherEntries),
  appraisalCycles: many(appraisalCycles),
  marketingCampaigns: many(marketingCampaigns),
  appraisalComments: many(appraisalComments),
  appraisalRatings: many(appraisalRatings),
  appraisals_employeeId: many(appraisals, {
    relationName: "appraisals_employeeId_users_id",
  }),
  appraisals_managerId: many(appraisals, {
    relationName: "appraisals_managerId_users_id",
  }),
  crmLeads: many(crmLeads),
  crmOpportunities: many(crmOpportunities),
  crmActivities: many(crmActivities),
  userGroups: many(userGroups),
  moduleOwners: many(moduleOwners),
  crmAccounts: many(crmAccounts),
  crmSettings: many(crmSettings),
  revenueAccounts: many(revenueAccounts),
  revenueLeads: many(revenueLeads),
  revenueOpportunities: many(revenueOpportunities),
  revenueActivities: many(revenueActivities),
  crmTasks: many(crmTasks),
  revenueSettings: many(revenueSettings),
  auditLogs: many(auditLog),
  approvalChainSteps: many(approvalChainSteps),
  sessions: many(sessions),
  revenueTasks: many(revenueTasks),
  messageHiddenFors: many(messageHiddenFor),
  messageReactions: many(messageReactions),
  trainingCompletions: many(trainingCompletions),
  legalAnnouncementAcks: many(legalAnnouncementAcks),
  userRoles: many(userRoles),
  userGroupMembers: many(userGroupMembers),
  moduleAccesses_userId: many(moduleAccess, {
    relationName: "moduleAccess_userId_users_id",
  }),
  moduleAccesses_grantedBy: many(moduleAccess, {
    relationName: "moduleAccess_grantedBy_users_id",
  }),
  conversationMembers: many(conversationMembers),
}));

export const conversationsRelations = relations(
  conversations,
  ({ one, many }) => ({
    user: one(users, {
      fields: [conversations.createdBy],
      references: [users.id],
    }),
    messages: many(messages),
    conversationMembers: many(conversationMembers),
  }),
);

export const messagesRelations = relations(messages, ({ one, many }) => ({
  conversation: one(conversations, {
    fields: [messages.conversationId],
    references: [conversations.id],
  }),
  user_authorId: one(users, {
    fields: [messages.authorId],
    references: [users.id],
    relationName: "messages_authorId_users_id",
  }),
  user_deletedBy: one(users, {
    fields: [messages.deletedBy],
    references: [users.id],
    relationName: "messages_deletedBy_users_id",
  }),
  messageHiddenFors: many(messageHiddenFor),
  messageReactions: many(messageReactions),
}));

export const wallCommentsRelations = relations(wallComments, ({ one }) => ({
  wallPost: one(wallPosts, {
    fields: [wallComments.postId],
    references: [wallPosts.id],
  }),
  user: one(users, {
    fields: [wallComments.authorId],
    references: [users.id],
  }),
}));

export const wallPostsRelations = relations(wallPosts, ({ one, many }) => ({
  wallComments: many(wallComments),
  user: one(users, {
    fields: [wallPosts.authorId],
    references: [users.id],
  }),
}));

export const companyNewsRelations = relations(companyNews, ({ one }) => ({
  user: one(users, {
    fields: [companyNews.authorId],
    references: [users.id],
  }),
}));

export const companyDatesRelations = relations(companyDates, ({ one }) => ({
  user: one(users, {
    fields: [companyDates.addedBy],
    references: [users.id],
  }),
}));

export const manutAiConversationSummariesRelations = relations(
  manutAiConversationSummaries,
  ({ one }) => ({
    manutAiConversation: one(manutAiConversations, {
      fields: [manutAiConversationSummaries.conversationId],
      references: [manutAiConversations.id],
    }),
  }),
);

export const manutAiConversationsRelations = relations(
  manutAiConversations,
  ({ one, many }) => ({
    manutAiConversationSummaries: many(manutAiConversationSummaries),
    manutAiConversationMemories: many(manutAiConversationMemory),
    manutAiMessages: many(manutAiMessages),
    user: one(users, {
      fields: [manutAiConversations.userId],
      references: [users.id],
    }),
    manutAiQueryLogs: many(manutAiQueryLogs),
  }),
);

export const manutAiConversationMemoryRelations = relations(
  manutAiConversationMemory,
  ({ one }) => ({
    manutAiConversation: one(manutAiConversations, {
      fields: [manutAiConversationMemory.conversationId],
      references: [manutAiConversations.id],
    }),
  }),
);

export const manutAiMessagesRelations = relations(
  manutAiMessages,
  ({ one, many }) => ({
    manutAiConversation: one(manutAiConversations, {
      fields: [manutAiMessages.conversationId],
      references: [manutAiConversations.id],
    }),
    manutAiAttachments: many(manutAiAttachments),
    manutAiFeedbacks: many(manutAiFeedback),
  }),
);

export const manutAiAttachmentsRelations = relations(
  manutAiAttachments,
  ({ one }) => ({
    user: one(users, {
      fields: [manutAiAttachments.userId],
      references: [users.id],
    }),
    manutAiMessage: one(manutAiMessages, {
      fields: [manutAiAttachments.messageId],
      references: [manutAiMessages.id],
    }),
  }),
);

export const manutAiKnowledgeArticlesRelations = relations(
  manutAiKnowledgeArticles,
  ({ one, many }) => ({
    user: one(users, {
      fields: [manutAiKnowledgeArticles.createdById],
      references: [users.id],
    }),
    manutAiFeedbacks: many(manutAiFeedback),
  }),
);

export const manutAiQueryLogsRelations = relations(manutAiQueryLogs, ({ one }) => ({
  user: one(users, {
    fields: [manutAiQueryLogs.userId],
    references: [users.id],
  }),
  manutAiConversation: one(manutAiConversations, {
    fields: [manutAiQueryLogs.conversationId],
    references: [manutAiConversations.id],
  }),
}));

export const manutAiBriefSubscriptionsRelations = relations(
  manutAiBriefSubscriptions,
  ({ one }) => ({
    user: one(users, {
      fields: [manutAiBriefSubscriptions.userId],
      references: [users.id],
    }),
  }),
);

export const manutAiBriefDeliveriesRelations = relations(
  manutAiBriefDeliveries,
  ({ one }) => ({
    user: one(users, {
      fields: [manutAiBriefDeliveries.userId],
      references: [users.id],
    }),
  }),
);

export const blogsRelations = relations(blogs, ({ one }) => ({
  user: one(users, {
    fields: [blogs.authorId],
    references: [users.id],
  }),
}));

export const manutAiFeedbackRelations = relations(manutAiFeedback, ({ one }) => ({
  manutAiMessage: one(manutAiMessages, {
    fields: [manutAiFeedback.messageId],
    references: [manutAiMessages.id],
  }),
  user_userId: one(users, {
    fields: [manutAiFeedback.userId],
    references: [users.id],
    relationName: "manutAiFeedback_userId_users_id",
  }),
  user_reviewedById: one(users, {
    fields: [manutAiFeedback.reviewedById],
    references: [users.id],
    relationName: "manutAiFeedback_reviewedById_users_id",
  }),
  manutAiKnowledgeArticle: one(manutAiKnowledgeArticles, {
    fields: [manutAiFeedback.resultingArticleId],
    references: [manutAiKnowledgeArticles.id],
  }),
}));

export const wikiPagesRelations = relations(wikiPages, ({ one, many }) => ({
  wikiPage: one(wikiPages, {
    fields: [wikiPages.parentId],
    references: [wikiPages.id],
    relationName: "wikiPages_parentId_wikiPages_id",
  }),
  wikiPages: many(wikiPages, {
    relationName: "wikiPages_parentId_wikiPages_id",
  }),
  user_createdById: one(users, {
    fields: [wikiPages.createdById],
    references: [users.id],
    relationName: "wikiPages_createdById_users_id",
  }),
  user_updatedById: one(users, {
    fields: [wikiPages.updatedById],
    references: [users.id],
    relationName: "wikiPages_updatedById_users_id",
  }),
  wikiPageVersions: many(wikiPageVersions),
  wikiPagePermissions: many(wikiPagePermissions),
}));

export const wikiPageVersionsRelations = relations(
  wikiPageVersions,
  ({ one }) => ({
    wikiPage: one(wikiPages, {
      fields: [wikiPageVersions.pageId],
      references: [wikiPages.id],
    }),
    user: one(users, {
      fields: [wikiPageVersions.createdById],
      references: [users.id],
    }),
  }),
);

export const wikiPagePermissionsRelations = relations(
  wikiPagePermissions,
  ({ one }) => ({
    wikiPage: one(wikiPages, {
      fields: [wikiPagePermissions.pageId],
      references: [wikiPages.id],
    }),
    user: one(users, {
      fields: [wikiPagePermissions.userId],
      references: [users.id],
    }),
  }),
);

export const userEntityMembershipsRelations = relations(
  userEntityMemberships,
  ({ one }) => ({
    user: one(users, {
      fields: [userEntityMemberships.userId],
      references: [users.id],
    }),
    entity: one(entities, {
      fields: [userEntityMemberships.entityId],
      references: [entities.id],
    }),
    role: one(roles, {
      fields: [userEntityMemberships.roleId],
      references: [roles.id],
    }),
  }),
);

export const entitiesRelations = relations(entities, ({ many }) => ({
  userEntityMemberships: many(userEntityMemberships),
  users: many(users),
  chartOfAccounts: many(chartOfAccounts),
  journalEntries: many(journalEntries),
  vendors: many(vendors),
  bankTransactions: many(bankTransactions),
  cashAdvanceRequests: many(cashAdvanceRequests),
  invoices: many(invoices),
  expenses: many(expenses),
  expenseReports: many(expenseReports),
  payments: many(payments),
  quotes: many(quotes),
  bankAccounts: many(bankAccounts),
  creditNotes: many(creditNotes),
  fiscalPeriods: many(fiscalPeriods),
  purchaseOrders: many(purchaseOrders),
  accountMappings: many(accountMappings),
  documentSequences: many(documentSequences),
  taxCodes: many(taxCodes),
  leaveTypes: many(leaveTypes),
  travelRequests: many(travelRequests),
  leaveRequests: many(leaveRequests),
  publicHolidays: many(publicHolidays),
  payrollRuns: many(payrollRuns),
  consultantInvoices: many(consultantInvoices),
  onboardingRuns: many(onboardingRuns),
  offboardingRuns: many(offboardingRuns),
  visaRecords: many(visaRecords),
  companyPolicies: many(companyPolicies),
  attendancePolicies: many(attendancePolicies),
  attendanceShifts: many(attendanceShifts),
  ninetyDayNotifications: many(ninetyDayNotifications),
  benefits: many(benefits),
  legalDocuments: many(legalDocuments),
  legalAnnouncements: many(legalAnnouncements),
}));

export const rolesRelations = relations(roles, ({ many }) => ({
  userEntityMemberships: many(userEntityMemberships),
  rolePermissions: many(rolePermissions),
  userRoles: many(userRoles),
}));

export const articlesRelations = relations(articles, ({ one }) => ({
  user: one(users, {
    fields: [articles.authorId],
    references: [users.id],
  }),
}));

export const departmentsRelations = relations(departments, ({ one, many }) => ({
  department: one(departments, {
    fields: [departments.parentId],
    references: [departments.id],
    relationName: "departments_parentId_departments_id",
  }),
  departments: many(departments, {
    relationName: "departments_parentId_departments_id",
  }),
}));

export const chartOfAccountsRelations = relations(
  chartOfAccounts,
  ({ one, many }) => ({
    entity: one(entities, {
      fields: [chartOfAccounts.entityId],
      references: [entities.id],
    }),
    chartOfAccount_parentId: one(chartOfAccounts, {
      fields: [chartOfAccounts.parentId],
      references: [chartOfAccounts.id],
      relationName: "chartOfAccounts_parentId_chartOfAccounts_id",
    }),
    chartOfAccounts_parentId: many(chartOfAccounts, {
      relationName: "chartOfAccounts_parentId_chartOfAccounts_id",
    }),
    chartOfAccount_reusedFromAccountId: one(chartOfAccounts, {
      fields: [chartOfAccounts.reusedFromAccountId],
      references: [chartOfAccounts.id],
      relationName: "chartOfAccounts_reusedFromAccountId_chartOfAccounts_id",
    }),
    chartOfAccounts_reusedFromAccountId: many(chartOfAccounts, {
      relationName: "chartOfAccounts_reusedFromAccountId_chartOfAccounts_id",
    }),
    bankTransactions_suggestedAccount: many(bankTransactions, {
      relationName: "bankTransactions_suggestedAccount_chartOfAccounts_id",
    }),
    bankTransactions_mappedAccount: many(bankTransactions, {
      relationName: "bankTransactions_mappedAccount_chartOfAccounts_id",
    }),
    journalEntryLines: many(journalEntryLines),
    bankAccounts: many(bankAccounts),
    accountMappings: many(accountMappings),
    taxCodes: many(taxCodes),
  }),
);

export const journalEntriesRelations = relations(
  journalEntries,
  ({ one, many }) => ({
    entity: one(entities, {
      fields: [journalEntries.entityId],
      references: [entities.id],
    }),
    user_createdBy: one(users, {
      fields: [journalEntries.createdBy],
      references: [users.id],
      relationName: "journalEntries_createdBy_users_id",
    }),
    user_approvedBy: one(users, {
      fields: [journalEntries.approvedBy],
      references: [users.id],
      relationName: "journalEntries_approvedBy_users_id",
    }),
    user_rejectedBy: one(users, {
      fields: [journalEntries.rejectedBy],
      references: [users.id],
      relationName: "journalEntries_rejectedBy_users_id",
    }),
    user_cancelledBy: one(users, {
      fields: [journalEntries.cancelledBy],
      references: [users.id],
      relationName: "journalEntries_cancelledBy_users_id",
    }),
    journalEntry_reversedByEntryId: one(journalEntries, {
      fields: [journalEntries.reversedByEntryId],
      references: [journalEntries.id],
      relationName: "journalEntries_reversedByEntryId_journalEntries_id",
    }),
    journalEntries_reversedByEntryId: many(journalEntries, {
      relationName: "journalEntries_reversedByEntryId_journalEntries_id",
    }),
    journalEntry_reversesEntryId: one(journalEntries, {
      fields: [journalEntries.reversesEntryId],
      references: [journalEntries.id],
      relationName: "journalEntries_reversesEntryId_journalEntries_id",
    }),
    journalEntries_reversesEntryId: many(journalEntries, {
      relationName: "journalEntries_reversesEntryId_journalEntries_id",
    }),
    journalEntryLines: many(journalEntryLines),
    invoices: many(invoices),
    payments: many(payments),
    fixedAssetDisposals: many(fixedAssetDisposals),
    fixedAssetRemeasurements: many(fixedAssetRemeasurements),
  }),
);

export const authLogsRelations = relations(authLogs, ({ one }) => ({
  user: one(users, {
    fields: [authLogs.userId],
    references: [users.id],
  }),
}));

export const vendorsRelations = relations(vendors, ({ one, many }) => ({
  entity: one(entities, {
    fields: [vendors.entityId],
    references: [entities.id],
  }),
  vendor: one(vendors, {
    fields: [vendors.mergedIntoId],
    references: [vendors.id],
    relationName: "vendors_mergedIntoId_vendors_id",
  }),
  vendors: many(vendors, {
    relationName: "vendors_mergedIntoId_vendors_id",
  }),
  invoices: many(invoices),
  quotes: many(quotes),
  creditNotes: many(creditNotes),
  purchaseOrders: many(purchaseOrders),
}));

export const bankTransactionsRelations = relations(
  bankTransactions,
  ({ one }) => ({
    entity: one(entities, {
      fields: [bankTransactions.entityId],
      references: [entities.id],
    }),
    bankAccount: one(bankAccounts, {
      fields: [bankTransactions.bankAccountId],
      references: [bankAccounts.id],
    }),
    chartOfAccount_suggestedAccount: one(chartOfAccounts, {
      fields: [bankTransactions.suggestedAccount],
      references: [chartOfAccounts.id],
      relationName: "bankTransactions_suggestedAccount_chartOfAccounts_id",
    }),
    chartOfAccount_mappedAccount: one(chartOfAccounts, {
      fields: [bankTransactions.mappedAccount],
      references: [chartOfAccounts.id],
      relationName: "bankTransactions_mappedAccount_chartOfAccounts_id",
    }),
    payment: one(payments, {
      fields: [bankTransactions.paymentId],
      references: [payments.id],
    }),
  }),
);

export const bankAccountsRelations = relations(
  bankAccounts,
  ({ one, many }) => ({
    bankTransactions: many(bankTransactions),
    payments: many(payments),
    entity: one(entities, {
      fields: [bankAccounts.entityId],
      references: [entities.id],
    }),
    chartOfAccount: one(chartOfAccounts, {
      fields: [bankAccounts.glAccountId],
      references: [chartOfAccounts.id],
    }),
  }),
);

export const paymentsRelations = relations(payments, ({ one, many }) => ({
  bankTransactions: many(bankTransactions),
  paymentAllocations: many(paymentAllocations),
  entity: one(entities, {
    fields: [payments.entityId],
    references: [entities.id],
  }),
  invoice: one(invoices, {
    fields: [payments.invoiceId],
    references: [invoices.id],
  }),
  bankAccount: one(bankAccounts, {
    fields: [payments.bankAccountId],
    references: [bankAccounts.id],
  }),
  journalEntry: one(journalEntries, {
    fields: [payments.linkedJeId],
    references: [journalEntries.id],
  }),
}));

export const cashAdvanceRequestsRelations = relations(
  cashAdvanceRequests,
  ({ one, many }) => ({
    user_employeeId: one(users, {
      fields: [cashAdvanceRequests.employeeId],
      references: [users.id],
      relationName: "cashAdvanceRequests_employeeId_users_id",
    }),
    entity: one(entities, {
      fields: [cashAdvanceRequests.entityId],
      references: [entities.id],
    }),
    user_approvedBy: one(users, {
      fields: [cashAdvanceRequests.approvedBy],
      references: [users.id],
      relationName: "cashAdvanceRequests_approvedBy_users_id",
    }),
    cashAdvanceApprovalDecisions: many(cashAdvanceApprovalDecisions),
    cashAdvanceItems: many(cashAdvanceItems),
  }),
);

export const invoiceLineItemsRelations = relations(
  invoiceLineItems,
  ({ one }) => ({
    invoice: one(invoices, {
      fields: [invoiceLineItems.invoiceId],
      references: [invoices.id],
    }),
  }),
);

export const invoicesRelations = relations(invoices, ({ one, many }) => ({
  invoiceLineItems: many(invoiceLineItems),
  entity: one(entities, {
    fields: [invoices.entityId],
    references: [entities.id],
  }),
  journalEntry: one(journalEntries, {
    fields: [invoices.linkedJeId],
    references: [journalEntries.id],
  }),
  vendor: one(vendors, {
    fields: [invoices.vendorId],
    references: [vendors.id],
  }),
  paymentAllocations: many(paymentAllocations),
  payments: many(payments),
  creditNotes: many(creditNotes),
}));

export const cashAdvanceApprovalStepsRelations = relations(
  cashAdvanceApprovalSteps,
  ({ one }) => ({
    user: one(users, {
      fields: [cashAdvanceApprovalSteps.approverUserId],
      references: [users.id],
    }),
  }),
);

export const journalEntryLinesRelations = relations(
  journalEntryLines,
  ({ one }) => ({
    journalEntry: one(journalEntries, {
      fields: [journalEntryLines.entryId],
      references: [journalEntries.id],
    }),
    chartOfAccount: one(chartOfAccounts, {
      fields: [journalEntryLines.accountId],
      references: [chartOfAccounts.id],
    }),
  }),
);

export const expensesRelations = relations(expenses, ({ one }) => ({
  user_employeeId: one(users, {
    fields: [expenses.employeeId],
    references: [users.id],
    relationName: "expenses_employeeId_users_id",
  }),
  entity: one(entities, {
    fields: [expenses.entityId],
    references: [entities.id],
  }),
  expenseCategory: one(expenseCategories, {
    fields: [expenses.categoryId],
    references: [expenseCategories.id],
  }),
  user_approvedBy: one(users, {
    fields: [expenses.approvedBy],
    references: [users.id],
    relationName: "expenses_approvedBy_users_id",
  }),
  travelRequest: one(travelRequests, {
    fields: [expenses.travelRequestId],
    references: [travelRequests.id],
  }),
  expenseReport: one(expenseReports, {
    fields: [expenses.reportId],
    references: [expenseReports.id],
  }),
}));

export const expenseCategoriesRelations = relations(
  expenseCategories,
  ({ many }) => ({
    expenses: many(expenses),
    cashAdvanceItems: many(cashAdvanceItems),
  }),
);

export const travelRequestsRelations = relations(
  travelRequests,
  ({ one, many }) => ({
    expenses: many(expenses),
    travelApprovalDecisions: many(travelApprovalDecisions),
    user_employeeId: one(users, {
      fields: [travelRequests.employeeId],
      references: [users.id],
      relationName: "travelRequests_employeeId_users_id",
    }),
    user_approvedBy: one(users, {
      fields: [travelRequests.approvedBy],
      references: [users.id],
      relationName: "travelRequests_approvedBy_users_id",
    }),
    user_delegatedTo: one(users, {
      fields: [travelRequests.delegatedTo],
      references: [users.id],
      relationName: "travelRequests_delegatedTo_users_id",
    }),
    entity: one(entities, {
      fields: [travelRequests.entityId],
      references: [entities.id],
    }),
  }),
);

export const expenseReportsRelations = relations(
  expenseReports,
  ({ one, many }) => ({
    expenses: many(expenses),
    user_employeeId: one(users, {
      fields: [expenseReports.employeeId],
      references: [users.id],
      relationName: "expenseReports_employeeId_users_id",
    }),
    entity: one(entities, {
      fields: [expenseReports.entityId],
      references: [entities.id],
    }),
    user_approvedBy: one(users, {
      fields: [expenseReports.approvedBy],
      references: [users.id],
      relationName: "expenseReports_approvedBy_users_id",
    }),
    expenseApprovalDecisions: many(expenseApprovalDecisions),
  }),
);

export const expenseApprovalStepsRelations = relations(
  expenseApprovalSteps,
  ({ one }) => ({
    user: one(users, {
      fields: [expenseApprovalSteps.approverUserId],
      references: [users.id],
    }),
  }),
);

export const expenseApprovalDecisionsRelations = relations(
  expenseApprovalDecisions,
  ({ one }) => ({
    expenseReport: one(expenseReports, {
      fields: [expenseApprovalDecisions.expenseReportId],
      references: [expenseReports.id],
    }),
    user_approverUserId: one(users, {
      fields: [expenseApprovalDecisions.approverUserId],
      references: [users.id],
      relationName: "expenseApprovalDecisions_approverUserId_users_id",
    }),
    user_decidedById: one(users, {
      fields: [expenseApprovalDecisions.decidedById],
      references: [users.id],
      relationName: "expenseApprovalDecisions_decidedById_users_id",
    }),
  }),
);

export const paymentAllocationsRelations = relations(
  paymentAllocations,
  ({ one }) => ({
    payment: one(payments, {
      fields: [paymentAllocations.paymentId],
      references: [payments.id],
    }),
    invoice: one(invoices, {
      fields: [paymentAllocations.invoiceId],
      references: [invoices.id],
    }),
  }),
);

export const cashAdvanceApprovalDecisionsRelations = relations(
  cashAdvanceApprovalDecisions,
  ({ one }) => ({
    cashAdvanceRequest: one(cashAdvanceRequests, {
      fields: [cashAdvanceApprovalDecisions.cashAdvanceRequestId],
      references: [cashAdvanceRequests.id],
    }),
    user_approverUserId: one(users, {
      fields: [cashAdvanceApprovalDecisions.approverUserId],
      references: [users.id],
      relationName: "cashAdvanceApprovalDecisions_approverUserId_users_id",
    }),
    user_decidedById: one(users, {
      fields: [cashAdvanceApprovalDecisions.decidedById],
      references: [users.id],
      relationName: "cashAdvanceApprovalDecisions_decidedById_users_id",
    }),
  }),
);

export const quotesRelations = relations(quotes, ({ one, many }) => ({
  entity: one(entities, {
    fields: [quotes.entityId],
    references: [entities.id],
  }),
  vendor: one(vendors, {
    fields: [quotes.vendorId],
    references: [vendors.id],
  }),
  quoteLines: many(quoteLines),
}));

export const quoteLinesRelations = relations(quoteLines, ({ one }) => ({
  quote: one(quotes, {
    fields: [quoteLines.quoteId],
    references: [quotes.id],
  }),
  taxCode: one(taxCodes, {
    fields: [quoteLines.taxCodeId],
    references: [taxCodes.id],
  }),
}));

export const taxCodesRelations = relations(taxCodes, ({ one, many }) => ({
  quoteLines: many(quoteLines),
  creditNoteLines: many(creditNoteLines),
  entity: one(entities, {
    fields: [taxCodes.entityId],
    references: [entities.id],
  }),
  chartOfAccount: one(chartOfAccounts, {
    fields: [taxCodes.glAccountId],
    references: [chartOfAccounts.id],
  }),
  poLines: many(poLines),
}));

export const cashAdvanceItemsRelations = relations(
  cashAdvanceItems,
  ({ one }) => ({
    cashAdvanceRequest: one(cashAdvanceRequests, {
      fields: [cashAdvanceItems.requestId],
      references: [cashAdvanceRequests.id],
    }),
    expenseCategory: one(expenseCategories, {
      fields: [cashAdvanceItems.categoryId],
      references: [expenseCategories.id],
    }),
  }),
);

export const creditNotesRelations = relations(creditNotes, ({ one, many }) => ({
  entity: one(entities, {
    fields: [creditNotes.entityId],
    references: [entities.id],
  }),
  vendor: one(vendors, {
    fields: [creditNotes.vendorId],
    references: [vendors.id],
  }),
  invoice: one(invoices, {
    fields: [creditNotes.linkedInvoiceId],
    references: [invoices.id],
  }),
  creditNoteLines: many(creditNoteLines),
}));

export const creditNoteLinesRelations = relations(
  creditNoteLines,
  ({ one }) => ({
    creditNote: one(creditNotes, {
      fields: [creditNoteLines.creditNoteId],
      references: [creditNotes.id],
    }),
    taxCode: one(taxCodes, {
      fields: [creditNoteLines.taxCodeId],
      references: [taxCodes.id],
    }),
  }),
);

export const fiscalPeriodsRelations = relations(fiscalPeriods, ({ one }) => ({
  entity: one(entities, {
    fields: [fiscalPeriods.entityId],
    references: [entities.id],
  }),
}));

export const purchaseOrdersRelations = relations(
  purchaseOrders,
  ({ one, many }) => ({
    entity: one(entities, {
      fields: [purchaseOrders.entityId],
      references: [entities.id],
    }),
    vendor: one(vendors, {
      fields: [purchaseOrders.vendorId],
      references: [vendors.id],
    }),
    poLines: many(poLines),
  }),
);

export const accountMappingsRelations = relations(
  accountMappings,
  ({ one }) => ({
    entity: one(entities, {
      fields: [accountMappings.entityId],
      references: [entities.id],
    }),
    chartOfAccount: one(chartOfAccounts, {
      fields: [accountMappings.chartOfAccountId],
      references: [chartOfAccounts.id],
    }),
  }),
);

export const documentSequencesRelations = relations(
  documentSequences,
  ({ one }) => ({
    entity: one(entities, {
      fields: [documentSequences.entityId],
      references: [entities.id],
    }),
  }),
);

export const poLinesRelations = relations(poLines, ({ one }) => ({
  purchaseOrder: one(purchaseOrders, {
    fields: [poLines.poId],
    references: [purchaseOrders.id],
  }),
  taxCode: one(taxCodes, {
    fields: [poLines.taxCodeId],
    references: [taxCodes.id],
  }),
}));

export const fixedAssetTransfersRelations = relations(
  fixedAssetTransfers,
  ({ one }) => ({
    fixedAsset_assetId: one(fixedAssets, {
      fields: [fixedAssetTransfers.assetId],
      references: [fixedAssets.id],
      relationName: "fixedAssetTransfers_assetId_fixedAssets_id",
    }),
    fixedAsset_destinationAssetId: one(fixedAssets, {
      fields: [fixedAssetTransfers.destinationAssetId],
      references: [fixedAssets.id],
      relationName: "fixedAssetTransfers_destinationAssetId_fixedAssets_id",
    }),
  }),
);

export const fixedAssetsRelations = relations(fixedAssets, ({ many }) => ({
  fixedAssetTransfers_assetId: many(fixedAssetTransfers, {
    relationName: "fixedAssetTransfers_assetId_fixedAssets_id",
  }),
  fixedAssetTransfers_destinationAssetId: many(fixedAssetTransfers, {
    relationName: "fixedAssetTransfers_destinationAssetId_fixedAssets_id",
  }),
  fixedAssetDisposals: many(fixedAssetDisposals),
  fixedAssetRemeasurements: many(fixedAssetRemeasurements),
  fixedAssetCountLines: many(fixedAssetCountLines),
}));

export const helpdeskTicketsRelations = relations(
  helpdeskTickets,
  ({ one, many }) => ({
    user_createdBy: one(users, {
      fields: [helpdeskTickets.createdBy],
      references: [users.id],
      relationName: "helpdeskTickets_createdBy_users_id",
    }),
    user_assigneeId: one(users, {
      fields: [helpdeskTickets.assigneeId],
      references: [users.id],
      relationName: "helpdeskTickets_assigneeId_users_id",
    }),
    helpdeskComments: many(helpdeskComments),
  }),
);

export const validatorNodeAlertsRelations = relations(
  validatorNodeAlerts,
  ({ one }) => ({
    user: one(users, {
      fields: [validatorNodeAlerts.createdBy],
      references: [users.id],
    }),
  }),
);

export const helpdeskSettingsRelations = relations(
  helpdeskSettings,
  ({ one }) => ({
    user: one(users, {
      fields: [helpdeskSettings.updatedBy],
      references: [users.id],
    }),
  }),
);

export const helpdeskCommentsRelations = relations(
  helpdeskComments,
  ({ one }) => ({
    helpdeskTicket: one(helpdeskTickets, {
      fields: [helpdeskComments.ticketId],
      references: [helpdeskTickets.id],
    }),
    user: one(users, {
      fields: [helpdeskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const fixedAssetDisposalsRelations = relations(
  fixedAssetDisposals,
  ({ one }) => ({
    fixedAsset: one(fixedAssets, {
      fields: [fixedAssetDisposals.assetId],
      references: [fixedAssets.id],
    }),
    journalEntry: one(journalEntries, {
      fields: [fixedAssetDisposals.linkedJeId],
      references: [journalEntries.id],
    }),
  }),
);

export const fixedAssetRemeasurementsRelations = relations(
  fixedAssetRemeasurements,
  ({ one }) => ({
    fixedAsset: one(fixedAssets, {
      fields: [fixedAssetRemeasurements.assetId],
      references: [fixedAssets.id],
    }),
    journalEntry: one(journalEntries, {
      fields: [fixedAssetRemeasurements.linkedJeId],
      references: [journalEntries.id],
    }),
  }),
);

export const surveyFormResponsesRelations = relations(
  surveyFormResponses,
  ({ one, many }) => ({
    surveyForm: one(surveyForms, {
      fields: [surveyFormResponses.surveyFormId],
      references: [surveyForms.id],
    }),
    user: one(users, {
      fields: [surveyFormResponses.respondentId],
      references: [users.id],
    }),
    surveyFormAnswers: many(surveyFormAnswers),
  }),
);

export const surveyFormsRelations = relations(surveyForms, ({ one, many }) => ({
  surveyFormResponses: many(surveyFormResponses),
  user: one(users, {
    fields: [surveyForms.createdById],
    references: [users.id],
  }),
  surveyFormQuestions: many(surveyFormQuestions),
}));

export const surveysRelations = relations(surveys, ({ one, many }) => ({
  user: one(users, {
    fields: [surveys.createdById],
    references: [users.id],
  }),
  surveyQuestions: many(surveyQuestions),
  surveyResponses: many(surveyResponses),
}));

export const surveyQuestionsRelations = relations(
  surveyQuestions,
  ({ one, many }) => ({
    survey: one(surveys, {
      fields: [surveyQuestions.surveyId],
      references: [surveys.id],
    }),
    surveyAnswers: many(surveyAnswers),
  }),
);

export const surveyResponsesRelations = relations(
  surveyResponses,
  ({ one, many }) => ({
    survey: one(surveys, {
      fields: [surveyResponses.surveyId],
      references: [surveys.id],
    }),
    user: one(users, {
      fields: [surveyResponses.respondentId],
      references: [users.id],
    }),
    surveyAnswers: many(surveyAnswers),
  }),
);

export const travelApprovalStepsRelations = relations(
  travelApprovalSteps,
  ({ one }) => ({
    user: one(users, {
      fields: [travelApprovalSteps.approverUserId],
      references: [users.id],
    }),
  }),
);

export const travelApprovalDecisionsRelations = relations(
  travelApprovalDecisions,
  ({ one }) => ({
    travelRequest: one(travelRequests, {
      fields: [travelApprovalDecisions.travelRequestId],
      references: [travelRequests.id],
    }),
    user_approverUserId: one(users, {
      fields: [travelApprovalDecisions.approverUserId],
      references: [users.id],
      relationName: "travelApprovalDecisions_approverUserId_users_id",
    }),
    user_decidedById: one(users, {
      fields: [travelApprovalDecisions.decidedById],
      references: [users.id],
      relationName: "travelApprovalDecisions_decidedById_users_id",
    }),
  }),
);

export const leaveTypesRelations = relations(leaveTypes, ({ one, many }) => ({
  entity: one(entities, {
    fields: [leaveTypes.entityId],
    references: [entities.id],
  }),
  balanceTransactions: many(balanceTransactions),
  leaveRequests: many(leaveRequests),
  leavePolicyApprovers: many(leavePolicyApprovers),
  leaveBalances: many(leaveBalances),
}));

export const surveyFormQuestionsRelations = relations(
  surveyFormQuestions,
  ({ one, many }) => ({
    surveyForm: one(surveyForms, {
      fields: [surveyFormQuestions.surveyFormId],
      references: [surveyForms.id],
    }),
    surveyFormAnswers: many(surveyFormAnswers),
  }),
);

export const surveyFormAnswersRelations = relations(
  surveyFormAnswers,
  ({ one }) => ({
    surveyFormResponse: one(surveyFormResponses, {
      fields: [surveyFormAnswers.responseId],
      references: [surveyFormResponses.id],
    }),
    surveyFormQuestion: one(surveyFormQuestions, {
      fields: [surveyFormAnswers.questionId],
      references: [surveyFormQuestions.id],
    }),
  }),
);

export const surveyAnswersRelations = relations(surveyAnswers, ({ one }) => ({
  surveyResponse: one(surveyResponses, {
    fields: [surveyAnswers.responseId],
    references: [surveyResponses.id],
  }),
  surveyQuestion: one(surveyQuestions, {
    fields: [surveyAnswers.questionId],
    references: [surveyQuestions.id],
  }),
}));

export const balanceTransactionsRelations = relations(
  balanceTransactions,
  ({ one }) => ({
    user: one(users, {
      fields: [balanceTransactions.employeeId],
      references: [users.id],
    }),
    leaveType: one(leaveTypes, {
      fields: [balanceTransactions.leaveTypeId],
      references: [leaveTypes.id],
    }),
  }),
);

export const leaveRequestsRelations = relations(
  leaveRequests,
  ({ one, many }) => ({
    user_employeeId: one(users, {
      fields: [leaveRequests.employeeId],
      references: [users.id],
      relationName: "leaveRequests_employeeId_users_id",
    }),
    leaveType: one(leaveTypes, {
      fields: [leaveRequests.leaveTypeId],
      references: [leaveTypes.id],
    }),
    user_approvedBy: one(users, {
      fields: [leaveRequests.approvedBy],
      references: [users.id],
      relationName: "leaveRequests_approvedBy_users_id",
    }),
    user_delegatedTo: one(users, {
      fields: [leaveRequests.delegatedTo],
      references: [users.id],
      relationName: "leaveRequests_delegatedTo_users_id",
    }),
    entity: one(entities, {
      fields: [leaveRequests.entityId],
      references: [entities.id],
    }),
    leaveApprovalDecisions: many(leaveApprovalDecisions),
  }),
);

export const leaveApprovalStepsRelations = relations(
  leaveApprovalSteps,
  ({ one }) => ({
    user: one(users, {
      fields: [leaveApprovalSteps.approverUserId],
      references: [users.id],
    }),
  }),
);

export const leaveApprovalDecisionsRelations = relations(
  leaveApprovalDecisions,
  ({ one }) => ({
    leaveRequest: one(leaveRequests, {
      fields: [leaveApprovalDecisions.leaveRequestId],
      references: [leaveRequests.id],
    }),
    user_approverUserId: one(users, {
      fields: [leaveApprovalDecisions.approverUserId],
      references: [users.id],
      relationName: "leaveApprovalDecisions_approverUserId_users_id",
    }),
    user_decidedById: one(users, {
      fields: [leaveApprovalDecisions.decidedById],
      references: [users.id],
      relationName: "leaveApprovalDecisions_decidedById_users_id",
    }),
  }),
);

export const publicHolidaysRelations = relations(publicHolidays, ({ one }) => ({
  entity: one(entities, {
    fields: [publicHolidays.entityId],
    references: [entities.id],
  }),
}));

export const payrollRunsRelations = relations(payrollRuns, ({ one, many }) => ({
  entity: one(entities, {
    fields: [payrollRuns.entityId],
    references: [entities.id],
  }),
  user_runBy: one(users, {
    fields: [payrollRuns.runBy],
    references: [users.id],
    relationName: "payrollRuns_runBy_users_id",
  }),
  user_approvedBy: one(users, {
    fields: [payrollRuns.approvedBy],
    references: [users.id],
    relationName: "payrollRuns_approvedBy_users_id",
  }),
  payslips: many(payslips),
}));

export const payslipsRelations = relations(payslips, ({ one }) => ({
  payrollRun: one(payrollRuns, {
    fields: [payslips.payrollRunId],
    references: [payrollRuns.id],
  }),
  user: one(users, {
    fields: [payslips.employeeId],
    references: [users.id],
  }),
}));

export const consultantInvoicesRelations = relations(
  consultantInvoices,
  ({ one }) => ({
    entity: one(entities, {
      fields: [consultantInvoices.entityId],
      references: [entities.id],
    }),
    user: one(users, {
      fields: [consultantInvoices.consultantId],
      references: [users.id],
    }),
  }),
);

export const esopGrantsRelations = relations(esopGrants, ({ one }) => ({
  user: one(users, {
    fields: [esopGrants.employeeId],
    references: [users.id],
  }),
}));

export const leavePolicyApproversRelations = relations(
  leavePolicyApprovers,
  ({ one }) => ({
    leaveType: one(leaveTypes, {
      fields: [leavePolicyApprovers.leaveTypeId],
      references: [leaveTypes.id],
    }),
    user: one(users, {
      fields: [leavePolicyApprovers.approverUserId],
      references: [users.id],
    }),
  }),
);

export const leaveBalancesRelations = relations(leaveBalances, ({ one }) => ({
  user: one(users, {
    fields: [leaveBalances.employeeId],
    references: [users.id],
  }),
  leaveType: one(leaveTypes, {
    fields: [leaveBalances.leaveTypeId],
    references: [leaveTypes.id],
  }),
}));

export const onboardingRunsRelations = relations(onboardingRuns, ({ one }) => ({
  user: one(users, {
    fields: [onboardingRuns.employeeId],
    references: [users.id],
  }),
  entity: one(entities, {
    fields: [onboardingRuns.entityId],
    references: [entities.id],
  }),
}));

export const offboardingRunsRelations = relations(
  offboardingRuns,
  ({ one }) => ({
    user: one(users, {
      fields: [offboardingRuns.employeeId],
      references: [users.id],
    }),
    entity: one(entities, {
      fields: [offboardingRuns.entityId],
      references: [entities.id],
    }),
  }),
);

export const visaRecordsRelations = relations(visaRecords, ({ one, many }) => ({
  user: one(users, {
    fields: [visaRecords.employeeId],
    references: [users.id],
  }),
  entity: one(entities, {
    fields: [visaRecords.entityId],
    references: [entities.id],
  }),
  visaEventLogs: many(visaEventLogs),
  visaChecklistItems: many(visaChecklistItems),
}));

export const visaEventLogsRelations = relations(visaEventLogs, ({ one }) => ({
  visaRecord: one(visaRecords, {
    fields: [visaEventLogs.visaRecordId],
    references: [visaRecords.id],
  }),
  user: one(users, {
    fields: [visaEventLogs.actorId],
    references: [users.id],
  }),
}));

export const visaKnowledgeArticlesRelations = relations(
  visaKnowledgeArticles,
  ({ one }) => ({
    user: one(users, {
      fields: [visaKnowledgeArticles.createdById],
      references: [users.id],
    }),
  }),
);

export const visaChecklistItemsRelations = relations(
  visaChecklistItems,
  ({ one }) => ({
    visaRecord: one(visaRecords, {
      fields: [visaChecklistItems.visaRecordId],
      references: [visaRecords.id],
    }),
  }),
);

export const employeeAgreementsRelations = relations(
  employeeAgreements,
  ({ one }) => ({
    user_employeeId: one(users, {
      fields: [employeeAgreements.employeeId],
      references: [users.id],
      relationName: "employeeAgreements_employeeId_users_id",
    }),
    user_uploadedById: one(users, {
      fields: [employeeAgreements.uploadedById],
      references: [users.id],
      relationName: "employeeAgreements_uploadedById_users_id",
    }),
  }),
);

export const benefitEnrollmentsRelations = relations(
  benefitEnrollments,
  ({ one }) => ({
    benefit: one(benefits, {
      fields: [benefitEnrollments.benefitId],
      references: [benefits.id],
    }),
    user: one(users, {
      fields: [benefitEnrollments.employeeId],
      references: [users.id],
    }),
  }),
);

export const benefitsRelations = relations(benefits, ({ one, many }) => ({
  benefitEnrollments: many(benefitEnrollments),
  entity: one(entities, {
    fields: [benefits.entityId],
    references: [entities.id],
  }),
}));

export const companyPoliciesRelations = relations(
  companyPolicies,
  ({ one }) => ({
    entity: one(entities, {
      fields: [companyPolicies.entityId],
      references: [entities.id],
    }),
    user: one(users, {
      fields: [companyPolicies.uploadedById],
      references: [users.id],
    }),
  }),
);

export const payrollApprovalStepsRelations = relations(
  payrollApprovalSteps,
  ({ one }) => ({
    user: one(users, {
      fields: [payrollApprovalSteps.approverUserId],
      references: [users.id],
    }),
  }),
);

export const attendancePoliciesRelations = relations(
  attendancePolicies,
  ({ one }) => ({
    entity: one(entities, {
      fields: [attendancePolicies.entityId],
      references: [entities.id],
    }),
  }),
);

export const attendanceCorrectionsRelations = relations(
  attendanceCorrections,
  ({ one }) => ({
    user_employeeId: one(users, {
      fields: [attendanceCorrections.employeeId],
      references: [users.id],
      relationName: "attendanceCorrections_employeeId_users_id",
    }),
    attendanceRecord: one(attendanceRecords, {
      fields: [attendanceCorrections.attendanceRecordId],
      references: [attendanceRecords.id],
    }),
    user_approvedBy: one(users, {
      fields: [attendanceCorrections.approvedBy],
      references: [users.id],
      relationName: "attendanceCorrections_approvedBy_users_id",
    }),
  }),
);

export const attendanceRecordsRelations = relations(
  attendanceRecords,
  ({ one, many }) => ({
    attendanceCorrections: many(attendanceCorrections),
    user: one(users, {
      fields: [attendanceRecords.employeeId],
      references: [users.id],
    }),
    attendanceAuditLogs: many(attendanceAuditLogs),
  }),
);

export const attendanceEmployeeShiftsRelations = relations(
  attendanceEmployeeShifts,
  ({ one }) => ({
    user: one(users, {
      fields: [attendanceEmployeeShifts.employeeId],
      references: [users.id],
    }),
    attendanceShift: one(attendanceShifts, {
      fields: [attendanceEmployeeShifts.shiftId],
      references: [attendanceShifts.id],
    }),
  }),
);

export const attendanceShiftsRelations = relations(
  attendanceShifts,
  ({ one, many }) => ({
    attendanceEmployeeShifts: many(attendanceEmployeeShifts),
    entity: one(entities, {
      fields: [attendanceShifts.entityId],
      references: [entities.id],
    }),
  }),
);

export const attendanceAuditLogsRelations = relations(
  attendanceAuditLogs,
  ({ one }) => ({
    attendanceRecord: one(attendanceRecords, {
      fields: [attendanceAuditLogs.recordId],
      references: [attendanceRecords.id],
    }),
    user_employeeId: one(users, {
      fields: [attendanceAuditLogs.employeeId],
      references: [users.id],
      relationName: "attendanceAuditLogs_employeeId_users_id",
    }),
    user_actorId: one(users, {
      fields: [attendanceAuditLogs.actorId],
      references: [users.id],
      relationName: "attendanceAuditLogs_actorId_users_id",
    }),
  }),
);

export const ninetyDayNotificationsRelations = relations(
  ninetyDayNotifications,
  ({ one }) => ({
    user: one(users, {
      fields: [ninetyDayNotifications.employeeId],
      references: [users.id],
    }),
    entity: one(entities, {
      fields: [ninetyDayNotifications.entityId],
      references: [entities.id],
    }),
  }),
);

export const attendanceExceptionsRelations = relations(
  attendanceExceptions,
  ({ one }) => ({
    user_employeeId: one(users, {
      fields: [attendanceExceptions.employeeId],
      references: [users.id],
      relationName: "attendanceExceptions_employeeId_users_id",
    }),
    user_approvedBy: one(users, {
      fields: [attendanceExceptions.approvedBy],
      references: [users.id],
      relationName: "attendanceExceptions_approvedBy_users_id",
    }),
  }),
);

export const userGoogleConnectionsRelations = relations(
  userGoogleConnections,
  ({ one }) => ({
    user: one(users, {
      fields: [userGoogleConnections.userId],
      references: [users.id],
    }),
  }),
);

export const investorsRelations = relations(investors, ({ one, many }) => ({
  user: one(users, {
    fields: [investors.addedBy],
    references: [users.id],
  }),
  investorTasks: many(investorTasks),
  investorActivities: many(investorActivities),
  investments: many(investments),
}));

export const investorTasksRelations = relations(investorTasks, ({ one }) => ({
  investor: one(investors, {
    fields: [investorTasks.investorId],
    references: [investors.id],
  }),
  user: one(users, {
    fields: [investorTasks.ownerId],
    references: [users.id],
  }),
}));

export const investorActivitiesRelations = relations(
  investorActivities,
  ({ one }) => ({
    investor: one(investors, {
      fields: [investorActivities.investorId],
      references: [investors.id],
    }),
    user: one(users, {
      fields: [investorActivities.ownerId],
      references: [users.id],
    }),
  }),
);

export const investorLeadsRelations = relations(investorLeads, ({ one }) => ({
  user: one(users, {
    fields: [investorLeads.ownerId],
    references: [users.id],
  }),
}));

export const investorAccountsRelations = relations(
  investorAccounts,
  ({ one, many }) => ({
    user: one(users, {
      fields: [investorAccounts.ownerId],
      references: [users.id],
    }),
    investorContacts: many(investorContacts),
  }),
);

export const certificatesRelations = relations(certificates, ({ one }) => ({
  user_recipientId: one(users, {
    fields: [certificates.recipientId],
    references: [users.id],
    relationName: "certificates_recipientId_users_id",
  }),
  user_issuedById: one(users, {
    fields: [certificates.issuedById],
    references: [users.id],
    relationName: "certificates_issuedById_users_id",
  }),
}));

export const investmentsRelations = relations(investments, ({ one }) => ({
  investor: one(investors, {
    fields: [investments.investorId],
    references: [investors.id],
  }),
}));

export const dataRoomDocumentsRelations = relations(
  dataRoomDocuments,
  ({ one }) => ({
    user: one(users, {
      fields: [dataRoomDocuments.uploadedBy],
      references: [users.id],
    }),
  }),
);

export const investorUpdatesRelations = relations(
  investorUpdates,
  ({ one }) => ({
    user: one(users, {
      fields: [investorUpdates.sentBy],
      references: [users.id],
    }),
  }),
);

export const itVendorsRelations = relations(itVendors, ({ one, many }) => ({
  user: one(users, {
    fields: [itVendors.createdBy],
    references: [users.id],
  }),
  itSubscriptions: many(itSubscriptions),
}));

export const itSubscriptionsRelations = relations(
  itSubscriptions,
  ({ one, many }) => ({
    itVendor: one(itVendors, {
      fields: [itSubscriptions.vendorId],
      references: [itVendors.id],
    }),
    user_ownerUserId: one(users, {
      fields: [itSubscriptions.ownerUserId],
      references: [users.id],
      relationName: "itSubscriptions_ownerUserId_users_id",
    }),
    user_createdBy: one(users, {
      fields: [itSubscriptions.createdBy],
      references: [users.id],
      relationName: "itSubscriptions_createdBy_users_id",
    }),
    user_renewalDecisionBy: one(users, {
      fields: [itSubscriptions.renewalDecisionBy],
      references: [users.id],
      relationName: "itSubscriptions_renewalDecisionBy_users_id",
    }),
    itBillingRecords: many(itBillingRecords),
    itBillingAlerts: many(itBillingAlerts),
  }),
);

export const itBillingRecordsRelations = relations(
  itBillingRecords,
  ({ one }) => ({
    itSubscription: one(itSubscriptions, {
      fields: [itBillingRecords.subscriptionId],
      references: [itSubscriptions.id],
    }),
    user: one(users, {
      fields: [itBillingRecords.createdBy],
      references: [users.id],
    }),
  }),
);

export const itBillingAlertsRelations = relations(
  itBillingAlerts,
  ({ one }) => ({
    itSubscription: one(itSubscriptions, {
      fields: [itBillingAlerts.subscriptionId],
      references: [itSubscriptions.id],
    }),
    user: one(users, {
      fields: [itBillingAlerts.acknowledgedBy],
      references: [users.id],
    }),
  }),
);

export const investorContactsRelations = relations(
  investorContacts,
  ({ one }) => ({
    user: one(users, {
      fields: [investorContacts.ownerId],
      references: [users.id],
    }),
    investorAccount: one(investorAccounts, {
      fields: [investorContacts.accountId],
      references: [investorAccounts.id],
    }),
  }),
);

export const itAccessAssignmentsRelations = relations(
  itAccessAssignments,
  ({ one }) => ({
    itAccessRequest: one(itAccessRequests, {
      fields: [itAccessAssignments.requestId],
      references: [itAccessRequests.id],
    }),
    user_employeeId: one(users, {
      fields: [itAccessAssignments.employeeId],
      references: [users.id],
      relationName: "itAccessAssignments_employeeId_users_id",
    }),
    itSystem: one(itSystems, {
      fields: [itAccessAssignments.systemId],
      references: [itSystems.id],
    }),
    user_grantedBy: one(users, {
      fields: [itAccessAssignments.grantedBy],
      references: [users.id],
      relationName: "itAccessAssignments_grantedBy_users_id",
    }),
    user_revokedBy: one(users, {
      fields: [itAccessAssignments.revokedBy],
      references: [users.id],
      relationName: "itAccessAssignments_revokedBy_users_id",
    }),
  }),
);

export const itAccessRequestsRelations = relations(
  itAccessRequests,
  ({ one, many }) => ({
    itAccessAssignments: many(itAccessAssignments),
    user_employeeId: one(users, {
      fields: [itAccessRequests.employeeId],
      references: [users.id],
      relationName: "itAccessRequests_employeeId_users_id",
    }),
    itSystem: one(itSystems, {
      fields: [itAccessRequests.systemId],
      references: [itSystems.id],
    }),
    user_grantedBy: one(users, {
      fields: [itAccessRequests.grantedBy],
      references: [users.id],
      relationName: "itAccessRequests_grantedBy_users_id",
    }),
    itAccessApprovalDecisions: many(itAccessApprovalDecisions),
  }),
);

export const itSystemsRelations = relations(itSystems, ({ many }) => ({
  itAccessAssignments: many(itAccessAssignments),
  itAccessRequests: many(itAccessRequests),
}));

export const itAccessAuditLogsRelations = relations(
  itAccessAuditLogs,
  ({ one }) => ({
    user_userId: one(users, {
      fields: [itAccessAuditLogs.userId],
      references: [users.id],
      relationName: "itAccessAuditLogs_userId_users_id",
    }),
    user_targetUserId: one(users, {
      fields: [itAccessAuditLogs.targetUserId],
      references: [users.id],
      relationName: "itAccessAuditLogs_targetUserId_users_id",
    }),
  }),
);

export const legalDocumentsRelations = relations(
  legalDocuments,
  ({ one, many }) => ({
    user: one(users, {
      fields: [legalDocuments.ownerId],
      references: [users.id],
    }),
    entity: one(entities, {
      fields: [legalDocuments.entityId],
      references: [entities.id],
    }),
    legalDocumentShares: many(legalDocumentShares),
    legalDocumentAttachments: many(legalDocumentAttachments),
    legalSignatures: many(legalSignatures),
  }),
);

export const legalDocumentSharesRelations = relations(
  legalDocumentShares,
  ({ one }) => ({
    legalDocument: one(legalDocuments, {
      fields: [legalDocumentShares.documentId],
      references: [legalDocuments.id],
    }),
    user_userId: one(users, {
      fields: [legalDocumentShares.userId],
      references: [users.id],
      relationName: "legalDocumentShares_userId_users_id",
    }),
    userGroup: one(userGroups, {
      fields: [legalDocumentShares.groupId],
      references: [userGroups.id],
    }),
    user_createdById: one(users, {
      fields: [legalDocumentShares.createdById],
      references: [users.id],
      relationName: "legalDocumentShares_createdById_users_id",
    }),
  }),
);

export const userGroupsRelations = relations(userGroups, ({ one, many }) => ({
  legalDocumentShares: many(legalDocumentShares),
  user: one(users, {
    fields: [userGroups.createdBy],
    references: [users.id],
  }),
  userGroupMembers: many(userGroupMembers),
}));

export const legalDocumentAttachmentsRelations = relations(
  legalDocumentAttachments,
  ({ one }) => ({
    legalDocument: one(legalDocuments, {
      fields: [legalDocumentAttachments.documentId],
      references: [legalDocuments.id],
    }),
    user: one(users, {
      fields: [legalDocumentAttachments.uploadedById],
      references: [users.id],
    }),
  }),
);

export const legalSignaturesRelations = relations(
  legalSignatures,
  ({ one }) => ({
    legalDocument: one(legalDocuments, {
      fields: [legalSignatures.documentId],
      references: [legalDocuments.id],
    }),
    user: one(users, {
      fields: [legalSignatures.createdById],
      references: [users.id],
    }),
  }),
);

export const userDocusignConnectionsRelations = relations(
  userDocusignConnections,
  ({ one }) => ({
    user: one(users, {
      fields: [userDocusignConnections.userId],
      references: [users.id],
    }),
  }),
);

export const legalAnnouncementsRelations = relations(
  legalAnnouncements,
  ({ one, many }) => ({
    entity: one(entities, {
      fields: [legalAnnouncements.entityId],
      references: [entities.id],
    }),
    user: one(users, {
      fields: [legalAnnouncements.authorId],
      references: [users.id],
    }),
    legalAnnouncementAttachments: many(legalAnnouncementAttachments),
    legalAnnouncementAcks: many(legalAnnouncementAcks),
  }),
);

export const legalAnnouncementAttachmentsRelations = relations(
  legalAnnouncementAttachments,
  ({ one }) => ({
    legalAnnouncement: one(legalAnnouncements, {
      fields: [legalAnnouncementAttachments.announcementId],
      references: [legalAnnouncements.id],
    }),
  }),
);

export const itAccessApprovalDecisionsRelations = relations(
  itAccessApprovalDecisions,
  ({ one }) => ({
    itAccessRequest: one(itAccessRequests, {
      fields: [itAccessApprovalDecisions.requestId],
      references: [itAccessRequests.id],
    }),
    user_approverUserId: one(users, {
      fields: [itAccessApprovalDecisions.approverUserId],
      references: [users.id],
      relationName: "itAccessApprovalDecisions_approverUserId_users_id",
    }),
    user_decidedById: one(users, {
      fields: [itAccessApprovalDecisions.decidedById],
      references: [users.id],
      relationName: "itAccessApprovalDecisions_decidedById_users_id",
    }),
  }),
);

export const mktCampaignLeversRelations = relations(
  mktCampaignLevers,
  ({ one }) => ({
    mktCampaign: one(mktCampaigns, {
      fields: [mktCampaignLevers.campaignId],
      references: [mktCampaigns.id],
    }),
    mktLever: one(mktLevers, {
      fields: [mktCampaignLevers.leverId],
      references: [mktLevers.id],
    }),
  }),
);

export const mktCampaignsRelations = relations(
  mktCampaigns,
  ({ one, many }) => ({
    mktCampaignLevers: many(mktCampaignLevers),
    mktCreatives: many(mktCreatives),
    mktPredictions: many(mktPredictions),
    user_ownerId: one(users, {
      fields: [mktCampaigns.ownerId],
      references: [users.id],
      relationName: "mktCampaigns_ownerId_users_id",
    }),
    user_createdBy: one(users, {
      fields: [mktCampaigns.createdBy],
      references: [users.id],
      relationName: "mktCampaigns_createdBy_users_id",
    }),
  }),
);

export const mktLeversRelations = relations(mktLevers, ({ many }) => ({
  mktCampaignLevers: many(mktCampaignLevers),
}));

export const mktCreativesRelations = relations(mktCreatives, ({ one }) => ({
  mktCampaign: one(mktCampaigns, {
    fields: [mktCreatives.campaignId],
    references: [mktCampaigns.id],
  }),
  user: one(users, {
    fields: [mktCreatives.uploadedBy],
    references: [users.id],
  }),
}));

export const mktPredictionsRelations = relations(mktPredictions, ({ one }) => ({
  mktCampaign: one(mktCampaigns, {
    fields: [mktPredictions.campaignId],
    references: [mktCampaigns.id],
  }),
  user: one(users, {
    fields: [mktPredictions.uploadedBy],
    references: [users.id],
  }),
}));

export const partnersRelations = relations(partners, ({ one, many }) => ({
  user: one(users, {
    fields: [partners.ownerId],
    references: [users.id],
  }),
  partnerContacts: many(partnerContacts),
  partnerMembers: many(partnerMembers),
  partnerColumns: many(partnerColumns),
  partnerTasks: many(partnerTasks),
  projects: many(projects),
  deals: many(deals),
  crmAccounts: many(crmAccounts),
  revenueAccounts: many(revenueAccounts),
}));

export const partnerContactsRelations = relations(
  partnerContacts,
  ({ one }) => ({
    partner: one(partners, {
      fields: [partnerContacts.partnerId],
      references: [partners.id],
    }),
  }),
);

export const partnerMembersRelations = relations(partnerMembers, ({ one }) => ({
  partner: one(partners, {
    fields: [partnerMembers.partnerId],
    references: [partners.id],
  }),
  user: one(users, {
    fields: [partnerMembers.userId],
    references: [users.id],
  }),
}));

export const partnerColumnsRelations = relations(partnerColumns, ({ one }) => ({
  partner: one(partners, {
    fields: [partnerColumns.partnerId],
    references: [partners.id],
  }),
}));

export const partnerTaskCommentsRelations = relations(
  partnerTaskComments,
  ({ one }) => ({
    partnerTask: one(partnerTasks, {
      fields: [partnerTaskComments.taskId],
      references: [partnerTasks.id],
    }),
    user: one(users, {
      fields: [partnerTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const partnerTasksRelations = relations(
  partnerTasks,
  ({ one, many }) => ({
    partnerTaskComments: many(partnerTaskComments),
    partnerTaskAssignees: many(partnerTaskAssignees),
    partner: one(partners, {
      fields: [partnerTasks.partnerId],
      references: [partners.id],
    }),
    user: one(users, {
      fields: [partnerTasks.ownerId],
      references: [users.id],
    }),
    partnerTask: one(partnerTasks, {
      fields: [partnerTasks.parentTaskId],
      references: [partnerTasks.id],
      relationName: "partnerTasks_parentTaskId_partnerTasks_id",
    }),
    partnerTasks: many(partnerTasks, {
      relationName: "partnerTasks_parentTaskId_partnerTasks_id",
    }),
    partnerTaskResources: many(partnerTaskResources),
  }),
);

export const partnerTaskAssigneesRelations = relations(
  partnerTaskAssignees,
  ({ one }) => ({
    partnerTask: one(partnerTasks, {
      fields: [partnerTaskAssignees.taskId],
      references: [partnerTasks.id],
    }),
    user: one(users, {
      fields: [partnerTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const projectWorkflowTransitionsRelations = relations(
  projectWorkflowTransitions,
  ({ one }) => ({
    project: one(projects, {
      fields: [projectWorkflowTransitions.projectId],
      references: [projects.id],
    }),
  }),
);

export const projectWorkflowEmailsRelations = relations(
  projectWorkflowEmails,
  ({ one }) => ({
    project: one(projects, {
      fields: [projectWorkflowEmails.projectId],
      references: [projects.id],
    }),
  }),
);

export const projectMembersRelations = relations(projectMembers, ({ one }) => ({
  project: one(projects, {
    fields: [projectMembers.projectId],
    references: [projects.id],
  }),
  user: one(users, {
    fields: [projectMembers.userId],
    references: [users.id],
  }),
}));

export const projectColumnsRelations = relations(projectColumns, ({ one }) => ({
  project: one(projects, {
    fields: [projectColumns.projectId],
    references: [projects.id],
  }),
}));

export const projectTasksRelations = relations(
  projectTasks,
  ({ one, many }) => ({
    project: one(projects, {
      fields: [projectTasks.projectId],
      references: [projects.id],
    }),
    projectMilestone: one(projectMilestones, {
      fields: [projectTasks.milestoneId],
      references: [projectMilestones.id],
    }),
    user: one(users, {
      fields: [projectTasks.ownerId],
      references: [users.id],
    }),
    projectTask: one(projectTasks, {
      fields: [projectTasks.parentTaskId],
      references: [projectTasks.id],
      relationName: "projectTasks_parentTaskId_projectTasks_id",
    }),
    projectTasks: many(projectTasks, {
      relationName: "projectTasks_parentTaskId_projectTasks_id",
    }),
    projectTaskComments: many(projectTaskComments),
    projectTaskAssignees: many(projectTaskAssignees),
    projectTaskActivities: many(projectTaskActivities),
    projectTaskResources: many(projectTaskResources),
    projectTaskDependencies_taskId: many(projectTaskDependencies, {
      relationName: "projectTaskDependencies_taskId_projectTasks_id",
    }),
    projectTaskDependencies_dependsOnTaskId: many(projectTaskDependencies, {
      relationName: "projectTaskDependencies_dependsOnTaskId_projectTasks_id",
    }),
  }),
);

export const projectMilestonesRelations = relations(
  projectMilestones,
  ({ one, many }) => ({
    projectTasks: many(projectTasks),
    project: one(projects, {
      fields: [projectMilestones.projectId],
      references: [projects.id],
    }),
    user: one(users, {
      fields: [projectMilestones.ownerId],
      references: [users.id],
    }),
  }),
);

export const projectTaskCommentsRelations = relations(
  projectTaskComments,
  ({ one }) => ({
    projectTask: one(projectTasks, {
      fields: [projectTaskComments.taskId],
      references: [projectTasks.id],
    }),
    user: one(users, {
      fields: [projectTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const partnerTaskResourcesRelations = relations(
  partnerTaskResources,
  ({ one }) => ({
    partnerTask: one(partnerTasks, {
      fields: [partnerTaskResources.taskId],
      references: [partnerTasks.id],
    }),
  }),
);

export const dealsRelations = relations(deals, ({ one }) => ({
  partner: one(partners, {
    fields: [deals.partnerId],
    references: [partners.id],
  }),
  user: one(users, {
    fields: [deals.ownerId],
    references: [users.id],
  }),
}));

export const projectTaskAssigneesRelations = relations(
  projectTaskAssignees,
  ({ one }) => ({
    projectTask: one(projectTasks, {
      fields: [projectTaskAssignees.taskId],
      references: [projectTasks.id],
    }),
    user: one(users, {
      fields: [projectTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const projectTaskActivitiesRelations = relations(
  projectTaskActivities,
  ({ one }) => ({
    projectTask: one(projectTasks, {
      fields: [projectTaskActivities.taskId],
      references: [projectTasks.id],
    }),
    user: one(users, {
      fields: [projectTaskActivities.actorId],
      references: [users.id],
    }),
  }),
);

export const projectTaskResourcesRelations = relations(
  projectTaskResources,
  ({ one }) => ({
    projectTask: one(projectTasks, {
      fields: [projectTaskResources.taskId],
      references: [projectTasks.id],
    }),
    user: one(users, {
      fields: [projectTaskResources.createdBy],
      references: [users.id],
    }),
  }),
);

export const itProjectsRelations = relations(itProjects, ({ one, many }) => ({
  user: one(users, {
    fields: [itProjects.ownerId],
    references: [users.id],
  }),
  itProjectMembers: many(itProjectMembers),
  itProjectColumns: many(itProjectColumns),
  itProjectTasks: many(itProjectTasks),
}));

export const itProjectMembersRelations = relations(
  itProjectMembers,
  ({ one }) => ({
    itProject: one(itProjects, {
      fields: [itProjectMembers.projectId],
      references: [itProjects.id],
    }),
    user: one(users, {
      fields: [itProjectMembers.userId],
      references: [users.id],
    }),
  }),
);

export const itProjectColumnsRelations = relations(
  itProjectColumns,
  ({ one }) => ({
    itProject: one(itProjects, {
      fields: [itProjectColumns.projectId],
      references: [itProjects.id],
    }),
  }),
);

export const itProjectTasksRelations = relations(
  itProjectTasks,
  ({ one, many }) => ({
    itProject: one(itProjects, {
      fields: [itProjectTasks.projectId],
      references: [itProjects.id],
    }),
    user: one(users, {
      fields: [itProjectTasks.ownerId],
      references: [users.id],
    }),
    itProjectTask: one(itProjectTasks, {
      fields: [itProjectTasks.parentTaskId],
      references: [itProjectTasks.id],
      relationName: "itProjectTasks_parentTaskId_itProjectTasks_id",
    }),
    itProjectTasks: many(itProjectTasks, {
      relationName: "itProjectTasks_parentTaskId_itProjectTasks_id",
    }),
    itProjectTaskComments: many(itProjectTaskComments),
    itProjectTaskAssignees: many(itProjectTaskAssignees),
  }),
);

export const itProjectTaskCommentsRelations = relations(
  itProjectTaskComments,
  ({ one }) => ({
    itProjectTask: one(itProjectTasks, {
      fields: [itProjectTaskComments.taskId],
      references: [itProjectTasks.id],
    }),
    user: one(users, {
      fields: [itProjectTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const itProjectTaskAssigneesRelations = relations(
  itProjectTaskAssignees,
  ({ one }) => ({
    itProjectTask: one(itProjectTasks, {
      fields: [itProjectTaskAssignees.taskId],
      references: [itProjectTasks.id],
    }),
    user: one(users, {
      fields: [itProjectTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const projectTaskDependenciesRelations = relations(
  projectTaskDependencies,
  ({ one }) => ({
    projectTask_taskId: one(projectTasks, {
      fields: [projectTaskDependencies.taskId],
      references: [projectTasks.id],
      relationName: "projectTaskDependencies_taskId_projectTasks_id",
    }),
    projectTask_dependsOnTaskId: one(projectTasks, {
      fields: [projectTaskDependencies.dependsOnTaskId],
      references: [projectTasks.id],
      relationName: "projectTaskDependencies_dependsOnTaskId_projectTasks_id",
    }),
  }),
);

export const qaProjectMembersRelations = relations(
  qaProjectMembers,
  ({ one }) => ({
    qaProject: one(qaProjects, {
      fields: [qaProjectMembers.projectId],
      references: [qaProjects.id],
    }),
    user: one(users, {
      fields: [qaProjectMembers.userId],
      references: [users.id],
    }),
  }),
);

export const qaProjectsRelations = relations(qaProjects, ({ one, many }) => ({
  qaProjectMembers: many(qaProjectMembers),
  qaProjectColumns: many(qaProjectColumns),
  qaProjectTasks: many(qaProjectTasks),
  user: one(users, {
    fields: [qaProjects.ownerId],
    references: [users.id],
  }),
}));

export const qaProjectColumnsRelations = relations(
  qaProjectColumns,
  ({ one }) => ({
    qaProject: one(qaProjects, {
      fields: [qaProjectColumns.projectId],
      references: [qaProjects.id],
    }),
  }),
);

export const qaProjectTasksRelations = relations(
  qaProjectTasks,
  ({ one, many }) => ({
    qaProject: one(qaProjects, {
      fields: [qaProjectTasks.projectId],
      references: [qaProjects.id],
    }),
    user: one(users, {
      fields: [qaProjectTasks.ownerId],
      references: [users.id],
    }),
    qaProjectTask: one(qaProjectTasks, {
      fields: [qaProjectTasks.parentTaskId],
      references: [qaProjectTasks.id],
      relationName: "qaProjectTasks_parentTaskId_qaProjectTasks_id",
    }),
    qaProjectTasks: many(qaProjectTasks, {
      relationName: "qaProjectTasks_parentTaskId_qaProjectTasks_id",
    }),
    qaProjectTaskComments: many(qaProjectTaskComments),
    qaProjectTaskAssignees: many(qaProjectTaskAssignees),
  }),
);

export const qaProjectTaskCommentsRelations = relations(
  qaProjectTaskComments,
  ({ one }) => ({
    qaProjectTask: one(qaProjectTasks, {
      fields: [qaProjectTaskComments.taskId],
      references: [qaProjectTasks.id],
    }),
    user: one(users, {
      fields: [qaProjectTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const qaProjectTaskAssigneesRelations = relations(
  qaProjectTaskAssignees,
  ({ one }) => ({
    qaProjectTask: one(qaProjectTasks, {
      fields: [qaProjectTaskAssignees.taskId],
      references: [qaProjectTasks.id],
    }),
    user: one(users, {
      fields: [qaProjectTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const legalProjectMembersRelations = relations(
  legalProjectMembers,
  ({ one }) => ({
    legalProject: one(legalProjects, {
      fields: [legalProjectMembers.projectId],
      references: [legalProjects.id],
    }),
    user: one(users, {
      fields: [legalProjectMembers.userId],
      references: [users.id],
    }),
  }),
);

export const legalProjectsRelations = relations(
  legalProjects,
  ({ one, many }) => ({
    legalProjectMembers: many(legalProjectMembers),
    legalProjectColumns: many(legalProjectColumns),
    legalProjectTasks: many(legalProjectTasks),
    user: one(users, {
      fields: [legalProjects.ownerId],
      references: [users.id],
    }),
  }),
);

export const legalProjectColumnsRelations = relations(
  legalProjectColumns,
  ({ one }) => ({
    legalProject: one(legalProjects, {
      fields: [legalProjectColumns.projectId],
      references: [legalProjects.id],
    }),
  }),
);

export const legalProjectTasksRelations = relations(
  legalProjectTasks,
  ({ one, many }) => ({
    legalProject: one(legalProjects, {
      fields: [legalProjectTasks.projectId],
      references: [legalProjects.id],
    }),
    user: one(users, {
      fields: [legalProjectTasks.ownerId],
      references: [users.id],
    }),
    legalProjectTask: one(legalProjectTasks, {
      fields: [legalProjectTasks.parentTaskId],
      references: [legalProjectTasks.id],
      relationName: "legalProjectTasks_parentTaskId_legalProjectTasks_id",
    }),
    legalProjectTasks: many(legalProjectTasks, {
      relationName: "legalProjectTasks_parentTaskId_legalProjectTasks_id",
    }),
    legalProjectTaskComments: many(legalProjectTaskComments),
    legalProjectTaskAssignees: many(legalProjectTaskAssignees),
  }),
);

export const legalProjectTaskCommentsRelations = relations(
  legalProjectTaskComments,
  ({ one }) => ({
    legalProjectTask: one(legalProjectTasks, {
      fields: [legalProjectTaskComments.taskId],
      references: [legalProjectTasks.id],
    }),
    user: one(users, {
      fields: [legalProjectTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const legalProjectTaskAssigneesRelations = relations(
  legalProjectTaskAssignees,
  ({ one }) => ({
    legalProjectTask: one(legalProjectTasks, {
      fields: [legalProjectTaskAssignees.taskId],
      references: [legalProjectTasks.id],
    }),
    user: one(users, {
      fields: [legalProjectTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const accountingProjectColumnsRelations = relations(
  accountingProjectColumns,
  ({ one }) => ({
    accountingProject: one(accountingProjects, {
      fields: [accountingProjectColumns.projectId],
      references: [accountingProjects.id],
    }),
  }),
);

export const accountingProjectsRelations = relations(
  accountingProjects,
  ({ one, many }) => ({
    accountingProjectColumns: many(accountingProjectColumns),
    accountingProjectTasks: many(accountingProjectTasks),
    user: one(users, {
      fields: [accountingProjects.ownerId],
      references: [users.id],
    }),
    accountingProjectMembers: many(accountingProjectMembers),
  }),
);

export const accountingProjectTasksRelations = relations(
  accountingProjectTasks,
  ({ one, many }) => ({
    accountingProject: one(accountingProjects, {
      fields: [accountingProjectTasks.projectId],
      references: [accountingProjects.id],
    }),
    user: one(users, {
      fields: [accountingProjectTasks.ownerId],
      references: [users.id],
    }),
    accountingProjectTask: one(accountingProjectTasks, {
      fields: [accountingProjectTasks.parentTaskId],
      references: [accountingProjectTasks.id],
      relationName:
        "accountingProjectTasks_parentTaskId_accountingProjectTasks_id",
    }),
    accountingProjectTasks: many(accountingProjectTasks, {
      relationName:
        "accountingProjectTasks_parentTaskId_accountingProjectTasks_id",
    }),
    accountingProjectTaskComments: many(accountingProjectTaskComments),
    accountingProjectTaskAssignees: many(accountingProjectTaskAssignees),
  }),
);

export const accountingProjectTaskCommentsRelations = relations(
  accountingProjectTaskComments,
  ({ one }) => ({
    accountingProjectTask: one(accountingProjectTasks, {
      fields: [accountingProjectTaskComments.taskId],
      references: [accountingProjectTasks.id],
    }),
    user: one(users, {
      fields: [accountingProjectTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const accountingProjectTaskAssigneesRelations = relations(
  accountingProjectTaskAssignees,
  ({ one }) => ({
    accountingProjectTask: one(accountingProjectTasks, {
      fields: [accountingProjectTaskAssignees.taskId],
      references: [accountingProjectTasks.id],
    }),
    user: one(users, {
      fields: [accountingProjectTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const productProjectMembersRelations = relations(
  productProjectMembers,
  ({ one }) => ({
    productProject: one(productProjects, {
      fields: [productProjectMembers.projectId],
      references: [productProjects.id],
    }),
    user: one(users, {
      fields: [productProjectMembers.userId],
      references: [users.id],
    }),
  }),
);

export const productProjectsRelations = relations(
  productProjects,
  ({ one, many }) => ({
    productProjectMembers: many(productProjectMembers),
    productProjectColumns: many(productProjectColumns),
    productProjectTasks: many(productProjectTasks),
    user: one(users, {
      fields: [productProjects.ownerId],
      references: [users.id],
    }),
  }),
);

export const productProjectColumnsRelations = relations(
  productProjectColumns,
  ({ one }) => ({
    productProject: one(productProjects, {
      fields: [productProjectColumns.projectId],
      references: [productProjects.id],
    }),
  }),
);

export const productProjectTasksRelations = relations(
  productProjectTasks,
  ({ one, many }) => ({
    productProject: one(productProjects, {
      fields: [productProjectTasks.projectId],
      references: [productProjects.id],
    }),
    user: one(users, {
      fields: [productProjectTasks.ownerId],
      references: [users.id],
    }),
    productProjectTask: one(productProjectTasks, {
      fields: [productProjectTasks.parentTaskId],
      references: [productProjectTasks.id],
      relationName: "productProjectTasks_parentTaskId_productProjectTasks_id",
    }),
    productProjectTasks: many(productProjectTasks, {
      relationName: "productProjectTasks_parentTaskId_productProjectTasks_id",
    }),
    productProjectTaskComments: many(productProjectTaskComments),
    productProjectTaskAssignees: many(productProjectTaskAssignees),
  }),
);

export const productProjectTaskCommentsRelations = relations(
  productProjectTaskComments,
  ({ one }) => ({
    productProjectTask: one(productProjectTasks, {
      fields: [productProjectTaskComments.taskId],
      references: [productProjectTasks.id],
    }),
    user: one(users, {
      fields: [productProjectTaskComments.authorId],
      references: [users.id],
    }),
  }),
);

export const productProjectTaskAssigneesRelations = relations(
  productProjectTaskAssignees,
  ({ one }) => ({
    productProjectTask: one(productProjectTasks, {
      fields: [productProjectTaskAssignees.taskId],
      references: [productProjectTasks.id],
    }),
    user: one(users, {
      fields: [productProjectTaskAssignees.userId],
      references: [users.id],
    }),
  }),
);

export const accountingProjectMembersRelations = relations(
  accountingProjectMembers,
  ({ one }) => ({
    accountingProject: one(accountingProjects, {
      fields: [accountingProjectMembers.projectId],
      references: [accountingProjects.id],
    }),
    user: one(users, {
      fields: [accountingProjectMembers.userId],
      references: [users.id],
    }),
  }),
);

export const deskBookingsRelations = relations(deskBookings, ({ one }) => ({
  officeDesk: one(officeDesks, {
    fields: [deskBookings.deskId],
    references: [officeDesks.id],
  }),
  user: one(users, {
    fields: [deskBookings.employeeId],
    references: [users.id],
  }),
}));

export const officeDesksRelations = relations(officeDesks, ({ one, many }) => ({
  deskBookings: many(deskBookings),
  office: one(offices, {
    fields: [officeDesks.officeId],
    references: [offices.id],
  }),
}));

export const roomBookingsRelations = relations(roomBookings, ({ one }) => ({
  meetingRoom: one(meetingRooms, {
    fields: [roomBookings.roomId],
    references: [meetingRooms.id],
  }),
  user: one(users, {
    fields: [roomBookings.employeeId],
    references: [users.id],
  }),
}));

export const meetingRoomsRelations = relations(
  meetingRooms,
  ({ one, many }) => ({
    roomBookings: many(roomBookings),
    office: one(offices, {
      fields: [meetingRooms.officeId],
      references: [offices.id],
    }),
  }),
);

export const assetsRelations = relations(assets, ({ one }) => ({
  office: one(offices, {
    fields: [assets.officeId],
    references: [offices.id],
  }),
  user: one(users, {
    fields: [assets.assignedTo],
    references: [users.id],
  }),
}));

export const officesRelations = relations(offices, ({ many }) => ({
  assets: many(assets),
  meetingRooms: many(meetingRooms),
  officeDesks: many(officeDesks),
}));

export const voucherEntriesRelations = relations(voucherEntries, ({ one }) => ({
  user: one(users, {
    fields: [voucherEntries.addedBy],
    references: [users.id],
  }),
}));

export const appraisalCyclesRelations = relations(
  appraisalCycles,
  ({ one, many }) => ({
    user: one(users, {
      fields: [appraisalCycles.createdBy],
      references: [users.id],
    }),
    appraisals: many(appraisals),
  }),
);

export const marketingCampaignsRelations = relations(
  marketingCampaigns,
  ({ one }) => ({
    user: one(users, {
      fields: [marketingCampaigns.addedBy],
      references: [users.id],
    }),
  }),
);

export const appraisalKrasRelations = relations(appraisalKras, ({ one }) => ({
  appraisal: one(appraisals, {
    fields: [appraisalKras.appraisalId],
    references: [appraisals.id],
  }),
  kraTemplate: one(kraTemplates, {
    fields: [appraisalKras.kraTemplateId],
    references: [kraTemplates.id],
  }),
}));

export const appraisalsRelations = relations(appraisals, ({ one, many }) => ({
  appraisalKras: many(appraisalKras),
  appraisalComments: many(appraisalComments),
  appraisalRatings: many(appraisalRatings),
  goals: many(goals),
  appraisalCycle: one(appraisalCycles, {
    fields: [appraisals.cycleId],
    references: [appraisalCycles.id],
  }),
  user_employeeId: one(users, {
    fields: [appraisals.employeeId],
    references: [users.id],
    relationName: "appraisals_employeeId_users_id",
  }),
  user_managerId: one(users, {
    fields: [appraisals.managerId],
    references: [users.id],
    relationName: "appraisals_managerId_users_id",
  }),
}));

export const kraTemplatesRelations = relations(kraTemplates, ({ many }) => ({
  appraisalKras: many(appraisalKras),
}));

export const appraisalCommentsRelations = relations(
  appraisalComments,
  ({ one }) => ({
    appraisal: one(appraisals, {
      fields: [appraisalComments.appraisalId],
      references: [appraisals.id],
    }),
    user: one(users, {
      fields: [appraisalComments.authorId],
      references: [users.id],
    }),
  }),
);

export const appraisalRatingsRelations = relations(
  appraisalRatings,
  ({ one }) => ({
    appraisal: one(appraisals, {
      fields: [appraisalRatings.appraisalId],
      references: [appraisals.id],
    }),
    user: one(users, {
      fields: [appraisalRatings.raterId],
      references: [users.id],
    }),
  }),
);

export const goalsRelations = relations(goals, ({ one }) => ({
  appraisal: one(appraisals, {
    fields: [goals.appraisalId],
    references: [appraisals.id],
  }),
}));

export const proposalInformationRequestsRelations = relations(
  proposalInformationRequests,
  ({ one }) => ({
    proposal: one(proposals, {
      fields: [proposalInformationRequests.proposalId],
      references: [proposals.id],
    }),
  }),
);

export const proposalTransitionsRelations = relations(
  proposalTransitions,
  ({ one }) => ({
    proposal: one(proposals, {
      fields: [proposalTransitions.proposalId],
      references: [proposals.id],
    }),
  }),
);

export const proposalEmailsRelations = relations(proposalEmails, ({ one }) => ({
  proposal: one(proposals, {
    fields: [proposalEmails.proposalId],
    references: [proposals.id],
  }),
}));

export const crmContactsRelations = relations(crmContacts, ({ one, many }) => ({
  crmAccount: one(crmAccounts, {
    fields: [crmContacts.accountId],
    references: [crmAccounts.id],
  }),
  crmOpportunities: many(crmOpportunities),
  crmActivities: many(crmActivities),
}));

export const crmAccountsRelations = relations(crmAccounts, ({ one, many }) => ({
  crmContacts: many(crmContacts),
  crmOpportunities: many(crmOpportunities),
  crmActivities: many(crmActivities),
  user: one(users, {
    fields: [crmAccounts.ownerId],
    references: [users.id],
  }),
  partner: one(partners, {
    fields: [crmAccounts.partnerId],
    references: [partners.id],
  }),
}));

export const crmLeadsRelations = relations(crmLeads, ({ one, many }) => ({
  user: one(users, {
    fields: [crmLeads.ownerId],
    references: [users.id],
  }),
  crmOpportunity: one(crmOpportunities, {
    fields: [crmLeads.convertedOpportunityId],
    references: [crmOpportunities.id],
  }),
  crmActivities: many(crmActivities),
  crmTasks: many(crmTasks),
}));

export const crmOpportunitiesRelations = relations(
  crmOpportunities,
  ({ one, many }) => ({
    crmLeads: many(crmLeads),
    crmAccount: one(crmAccounts, {
      fields: [crmOpportunities.accountId],
      references: [crmAccounts.id],
    }),
    crmContact: one(crmContacts, {
      fields: [crmOpportunities.contactId],
      references: [crmContacts.id],
    }),
    user: one(users, {
      fields: [crmOpportunities.ownerId],
      references: [users.id],
    }),
    crmActivities: many(crmActivities),
    crmOpportunityBusinessUnits: many(crmOpportunityBusinessUnits),
    crmTasks: many(crmTasks),
  }),
);

export const crmActivitiesRelations = relations(crmActivities, ({ one }) => ({
  user: one(users, {
    fields: [crmActivities.ownerId],
    references: [users.id],
  }),
  crmLead: one(crmLeads, {
    fields: [crmActivities.leadId],
    references: [crmLeads.id],
  }),
  crmOpportunity: one(crmOpportunities, {
    fields: [crmActivities.opportunityId],
    references: [crmOpportunities.id],
  }),
  crmContact: one(crmContacts, {
    fields: [crmActivities.contactId],
    references: [crmContacts.id],
  }),
  crmAccount: one(crmAccounts, {
    fields: [crmActivities.accountId],
    references: [crmAccounts.id],
  }),
}));

export const moduleOwnersRelations = relations(moduleOwners, ({ one }) => ({
  user: one(users, {
    fields: [moduleOwners.ownerId],
    references: [users.id],
  }),
}));

export const crmSettingsRelations = relations(crmSettings, ({ one }) => ({
  user: one(users, {
    fields: [crmSettings.updatedBy],
    references: [users.id],
  }),
}));

export const crmOpportunityBusinessUnitsRelations = relations(
  crmOpportunityBusinessUnits,
  ({ one }) => ({
    crmOpportunity: one(crmOpportunities, {
      fields: [crmOpportunityBusinessUnits.opportunityId],
      references: [crmOpportunities.id],
    }),
  }),
);

export const revenueAccountsRelations = relations(
  revenueAccounts,
  ({ one, many }) => ({
    user: one(users, {
      fields: [revenueAccounts.ownerId],
      references: [users.id],
    }),
    partner: one(partners, {
      fields: [revenueAccounts.partnerId],
      references: [partners.id],
    }),
    revenueContacts: many(revenueContacts),
    revenueOpportunities: many(revenueOpportunities),
    revenueActivities: many(revenueActivities),
  }),
);

export const revenueContactsRelations = relations(
  revenueContacts,
  ({ one, many }) => ({
    revenueAccount: one(revenueAccounts, {
      fields: [revenueContacts.accountId],
      references: [revenueAccounts.id],
    }),
    revenueOpportunities: many(revenueOpportunities),
    revenueActivities: many(revenueActivities),
  }),
);

export const revenueLeadsRelations = relations(
  revenueLeads,
  ({ one, many }) => ({
    user: one(users, {
      fields: [revenueLeads.ownerId],
      references: [users.id],
    }),
    revenueOpportunity: one(revenueOpportunities, {
      fields: [revenueLeads.convertedOpportunityId],
      references: [revenueOpportunities.id],
    }),
    revenueActivities: many(revenueActivities),
    revenueTasks: many(revenueTasks),
  }),
);

export const revenueOpportunitiesRelations = relations(
  revenueOpportunities,
  ({ one, many }) => ({
    revenueLeads: many(revenueLeads),
    revenueAccount: one(revenueAccounts, {
      fields: [revenueOpportunities.accountId],
      references: [revenueAccounts.id],
    }),
    revenueContact: one(revenueContacts, {
      fields: [revenueOpportunities.contactId],
      references: [revenueContacts.id],
    }),
    user: one(users, {
      fields: [revenueOpportunities.ownerId],
      references: [users.id],
    }),
    revenueActivities: many(revenueActivities),
    revenueOpportunityBusinessUnits: many(revenueOpportunityBusinessUnits),
    revenueTasks: many(revenueTasks),
  }),
);

export const revenueActivitiesRelations = relations(
  revenueActivities,
  ({ one }) => ({
    user: one(users, {
      fields: [revenueActivities.ownerId],
      references: [users.id],
    }),
    revenueLead: one(revenueLeads, {
      fields: [revenueActivities.leadId],
      references: [revenueLeads.id],
    }),
    revenueOpportunity: one(revenueOpportunities, {
      fields: [revenueActivities.opportunityId],
      references: [revenueOpportunities.id],
    }),
    revenueContact: one(revenueContacts, {
      fields: [revenueActivities.contactId],
      references: [revenueContacts.id],
    }),
    revenueAccount: one(revenueAccounts, {
      fields: [revenueActivities.accountId],
      references: [revenueAccounts.id],
    }),
  }),
);

export const crmTasksRelations = relations(crmTasks, ({ one }) => ({
  user: one(users, {
    fields: [crmTasks.ownerId],
    references: [users.id],
  }),
  crmOpportunity: one(crmOpportunities, {
    fields: [crmTasks.opportunityId],
    references: [crmOpportunities.id],
  }),
  crmLead: one(crmLeads, {
    fields: [crmTasks.leadId],
    references: [crmLeads.id],
  }),
}));

export const revenueSettingsRelations = relations(
  revenueSettings,
  ({ one }) => ({
    user: one(users, {
      fields: [revenueSettings.updatedBy],
      references: [users.id],
    }),
  }),
);

export const revenueOpportunityBusinessUnitsRelations = relations(
  revenueOpportunityBusinessUnits,
  ({ one }) => ({
    revenueOpportunity: one(revenueOpportunities, {
      fields: [revenueOpportunityBusinessUnits.opportunityId],
      references: [revenueOpportunities.id],
    }),
  }),
);

export const auditLogRelations = relations(auditLog, ({ one }) => ({
  user: one(users, {
    fields: [auditLog.userId],
    references: [users.id],
  }),
}));

export const approvalChainStepsRelations = relations(
  approvalChainSteps,
  ({ one }) => ({
    approvalChain: one(approvalChains, {
      fields: [approvalChainSteps.chainId],
      references: [approvalChains.id],
    }),
    user: one(users, {
      fields: [approvalChainSteps.approverUserId],
      references: [users.id],
    }),
  }),
);

export const approvalChainsRelations = relations(
  approvalChains,
  ({ many }) => ({
    approvalChainSteps: many(approvalChainSteps),
  }),
);

export const applicationsRelations = relations(applications, ({ one }) => ({
  job: one(jobs, {
    fields: [applications.jobId],
    references: [jobs.id],
  }),
}));

export const jobsRelations = relations(jobs, ({ many }) => ({
  applications: many(applications),
}));

export const sessionsRelations = relations(sessions, ({ one }) => ({
  user: one(users, {
    fields: [sessions.userId],
    references: [users.id],
  }),
}));

export const fixedAssetCountLinesRelations = relations(
  fixedAssetCountLines,
  ({ one }) => ({
    fixedAssetCountSession: one(fixedAssetCountSessions, {
      fields: [fixedAssetCountLines.sessionId],
      references: [fixedAssetCountSessions.id],
    }),
    fixedAsset: one(fixedAssets, {
      fields: [fixedAssetCountLines.assetId],
      references: [fixedAssets.id],
    }),
  }),
);

export const fixedAssetCountSessionsRelations = relations(
  fixedAssetCountSessions,
  ({ many }) => ({
    fixedAssetCountLines: many(fixedAssetCountLines),
  }),
);

export const revenueTasksRelations = relations(revenueTasks, ({ one }) => ({
  user: one(users, {
    fields: [revenueTasks.ownerId],
    references: [users.id],
  }),
  revenueOpportunity: one(revenueOpportunities, {
    fields: [revenueTasks.opportunityId],
    references: [revenueOpportunities.id],
  }),
  revenueLead: one(revenueLeads, {
    fields: [revenueTasks.leadId],
    references: [revenueLeads.id],
  }),
}));

export const rolePermissionsRelations = relations(
  rolePermissions,
  ({ one }) => ({
    role: one(roles, {
      fields: [rolePermissions.roleId],
      references: [roles.id],
    }),
  }),
);

export const messageHiddenForRelations = relations(
  messageHiddenFor,
  ({ one }) => ({
    message: one(messages, {
      fields: [messageHiddenFor.messageId],
      references: [messages.id],
    }),
    user: one(users, {
      fields: [messageHiddenFor.userId],
      references: [users.id],
    }),
  }),
);

export const messageReactionsRelations = relations(
  messageReactions,
  ({ one }) => ({
    message: one(messages, {
      fields: [messageReactions.messageId],
      references: [messages.id],
    }),
    user: one(users, {
      fields: [messageReactions.userId],
      references: [users.id],
    }),
  }),
);

export const trainingCompletionsRelations = relations(
  trainingCompletions,
  ({ one }) => ({
    user: one(users, {
      fields: [trainingCompletions.employeeId],
      references: [users.id],
    }),
    trainingModule: one(trainingModules, {
      fields: [trainingCompletions.moduleId],
      references: [trainingModules.id],
    }),
  }),
);

export const trainingModulesRelations = relations(
  trainingModules,
  ({ many }) => ({
    trainingCompletions: many(trainingCompletions),
  }),
);

export const legalAnnouncementAcksRelations = relations(
  legalAnnouncementAcks,
  ({ one }) => ({
    legalAnnouncement: one(legalAnnouncements, {
      fields: [legalAnnouncementAcks.announcementId],
      references: [legalAnnouncements.id],
    }),
    user: one(users, {
      fields: [legalAnnouncementAcks.userId],
      references: [users.id],
    }),
  }),
);

export const userRolesRelations = relations(userRoles, ({ one }) => ({
  user: one(users, {
    fields: [userRoles.userId],
    references: [users.id],
  }),
  role: one(roles, {
    fields: [userRoles.roleId],
    references: [roles.id],
  }),
}));

export const userGroupMembersRelations = relations(
  userGroupMembers,
  ({ one }) => ({
    userGroup: one(userGroups, {
      fields: [userGroupMembers.groupId],
      references: [userGroups.id],
    }),
    user: one(users, {
      fields: [userGroupMembers.userId],
      references: [users.id],
    }),
  }),
);

export const moduleAccessRelations = relations(moduleAccess, ({ one }) => ({
  user_userId: one(users, {
    fields: [moduleAccess.userId],
    references: [users.id],
    relationName: "moduleAccess_userId_users_id",
  }),
  user_grantedBy: one(users, {
    fields: [moduleAccess.grantedBy],
    references: [users.id],
    relationName: "moduleAccess_grantedBy_users_id",
  }),
}));

export const conversationMembersRelations = relations(
  conversationMembers,
  ({ one }) => ({
    conversation: one(conversations, {
      fields: [conversationMembers.conversationId],
      references: [conversations.id],
    }),
    user: one(users, {
      fields: [conversationMembers.userId],
      references: [users.id],
    }),
  }),
);
